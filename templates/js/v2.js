(function(){var html=document.documentElement;var reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;var RE=/^[A-Za-z][A-Za-z0-9_]{2,23}$/;function esc(s){return s.replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
function $(s,r){return(r||document).querySelector(s);}
function $$(s,r){return[].slice.call((r||document).querySelectorAll(s));}
var nav=$('.nav');function onScroll(){if(nav)nav.classList.toggle('is-scrolled',scrollY>24);}
onScroll();addEventListener('scroll',onScroll,{passive:true});var burger=$('.nav__burger'),menu=$('#mobile-menu');function setMenu(open){menu.hidden=!open;burger.classList.toggle('is-open',open);burger.setAttribute('aria-expanded',String(open));burger.setAttribute('aria-label',open?'Close menu':'Open menu');document.body.style.overflow=open?'hidden':'';if(open){menu.setAttribute('role','dialog');menu.setAttribute('aria-modal','true');menu.setAttribute('aria-label','Menu');}else{menu.removeAttribute('role');menu.removeAttribute('aria-modal');menu.removeAttribute('aria-label');}
$$('body > *').forEach(function(el){if(el===menu||el.contains(burger)||/^(SCRIPT|STYLE|SVG)$/.test(el.tagName))return;if(open)el.setAttribute('inert','');else el.removeAttribute('inert');});if(!open&&document.activeElement&&(menu.contains(document.activeElement)||document.activeElement===document.body))burger.focus();}
if(burger&&menu){burger.addEventListener('click',function(){setMenu(menu.hidden);});$$('a',menu).forEach(function(a){a.addEventListener('click',function(){setMenu(false);});});addEventListener('keydown',function(e){if(e.key==='Escape'&&!menu.hidden){setMenu(false);burger.focus();}});addEventListener('keydown',function(e){if(e.key!=='Tab'||menu.hidden)return;var f=[burger].concat($$('a',menu));var i=f.indexOf(document.activeElement);if(i<0){e.preventDefault();f[0].focus();return;}if(!e.shiftKey&&i===f.length-1){e.preventDefault();f[0].focus();}else if(e.shiftKey&&i===0){e.preventDefault();f[f.length-1].focus();}});}
var phone=$('#hero-phone');var THEMES=['gothic','soft','mono','y2k','sunset','paper','vampire','cathedral','static','lavender'];var moreLink=$('[data-wall-more]'),moreBase=moreLink?(moreLink.getAttribute('href')||'/explore').split('?')[0]:'',themePicked=false;function syncMore(t){if(!moreLink)return;var lab=$('[data-more-label]',moreLink);if(!themePicked){moreLink.setAttribute('href',moreBase);if(lab)lab.textContent='All sixteen on explore';return;}moreLink.setAttribute('href',moreBase+'?theme='+encodeURIComponent(t));if(lab)lab.textContent='More '+t+' pages on explore';}
function setTheme(t){if(!phone||THEMES.indexOf(t)<0)return;THEMES.forEach(function(x){phone.classList.remove('theme-'+x);});phone.classList.add('theme-'+t);phone.dataset.theme=t;$$('[data-set-theme]').forEach(function(b){var on=b.dataset.setTheme===t;if(b.getAttribute('role')==='radio'){b.setAttribute('aria-checked',String(on));b.tabIndex=on?0:-1;}else b.setAttribute('aria-pressed',String(on));});syncMore(t);}
$$('[data-set-theme]').forEach(function(b){b.addEventListener('click',function(){themePicked=true;setTheme(b.dataset.setTheme);});});$$('.swatches[role="radiogroup"]').forEach(function(g){g.addEventListener('keydown',function(e){var keys={ArrowRight:1,ArrowDown:1,ArrowLeft:-1,ArrowUp:-1,Home:'first',End:'last'};if(!(e.key in keys))return;var items=$$('[role="radio"]',g),i=items.indexOf(document.activeElement);if(i<0)return;e.preventDefault();var j=keys[e.key]==='first'?0:keys[e.key]==='last'?items.length-1:(i+keys[e.key]+items.length)%items.length;items[j].focus();themePicked=true;setTheme(items[j].dataset.setTheme);});});var bioEl=phone&&$('[data-bio]',phone),bioCaret=phone&&$('.pf__bio .caret',phone);var bioTyper=null;function makeTyper(el,words,opts){opts=opts||{};var i=0,alive=true,timer=null;function wait(ms){return new Promise(function(r){timer=setTimeout(r,ms);});}
(async function run(){while(alive){var w=words[i%words.length];for(var k=1;k<=w.length&&alive;k++){el.textContent=w.slice(0,k);await wait(opts.type||70);}
await wait(opts.hold||1700);for(var j=w.length;j>=0&&alive;j--){el.textContent=w.slice(0,j);await wait(opts.erase||38);}
await wait(280);i++;}})();return{stop:function(text){alive=false;clearTimeout(timer);if(text!=null)el.textContent=text;}};}
var BIOS=['your bio, typing itself.','producer. night owl. not a phase.','links, music, socials — all in one place.'];function setFx(name,on){if(!phone)return;if(name==='video'){phone.classList.toggle('video-on',on);var v=$('.phone__video',phone);if(v){if(on&&!reduce){if(document.readyState==='complete'||!v.hasAttribute('data-autoplay'))v.play&&v.play().catch(function(){});}else{v.pause&&v.pause();}}}
if(name==='type'&&bioEl&&bioCaret){if(on&&!reduce){if(!bioTyper)bioTyper=makeTyper(bioEl,BIOS);bioCaret.hidden=false;}
else{if(bioTyper){bioTyper.stop(BIOS[0]);bioTyper=null;}else{bioEl.textContent=BIOS[0];}bioCaret.hidden=true;}}
if(name==='sparkle'){phone.classList.toggle('is-sparkle',on);phone.classList.toggle('is-shimmer',on);$('.pf__name',phone).classList.toggle('fx-sparkle',on);$('[data-badge]',phone).classList.toggle('pf__badge--gold',on);}
$$('[data-fx="'+name+'"]').forEach(function(b){b.setAttribute(b.getAttribute('role')==='switch'?'aria-checked':'aria-pressed',String(on));});}
$$('[data-fx]').forEach(function(b){b.addEventListener('click',function(){setFx(b.dataset.fx,(b.getAttribute('aria-checked')||b.getAttribute('aria-pressed'))!=='true');});});if(phone){setFx('video',true);setFx('type',true);setFx('sparkle',false);}
var live=$('[data-live]');function setName(v){if(!phone)return;var n=v||'yourname';$('[data-name]',phone).textContent=n;$('[data-handle]',phone).textContent=n;$('[data-artist]',phone).textContent=n;$('[data-avatar]',phone).textContent=n.charAt(0).toLowerCase();}
var stage=$('.hero__stage');if(stage&&phone&&!reduce&&matchMedia('(hover:hover) and (min-width:941px)').matches){stage.addEventListener('pointermove',function(e){var r=stage.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5, y=(e.clientY-r.top)/r.height-.5;phone.style.transform='rotateY('+(-12+x*16)+'deg) rotateX('+(3-y*10)+'deg)';});stage.addEventListener('pointerleave',function(){phone.style.transform='';});}
var ON_SITE=/(^|\.)misa\.lol$/.test(location.hostname)||location.hostname==='localhost'||location.hostname==='misa.test';var availTimer=null,availLast={},availWait={name:null,cbs:[]};function checkAvail(name,cb){if(!ON_SITE||!window.fetch)return;clearTimeout(availTimer);if(availLast.name===name){cb(availLast.res);return;}
if(availWait.name!==name)availWait={name:name,cbs:[]};availWait.cbs.push(cb);availTimer=setTimeout(function(){var ask=availWait;availWait={name:null,cbs:[]};fetch('/api/v1/auth/available?username='+encodeURIComponent(name),{headers:{Accept:'application/json'}}).then(function(r){return r.ok?r.json():null;}).then(function(res){if(!res)return;availLast={name:name,res:res};ask.cbs.forEach(function(f){f(res);});}).catch(function(){});},350);}
function availHint(name,res){if(!res||res.available==null)return null;name=name.toLowerCase();if(res.available)return'misa.lol/<b>'+esc(name)+'</b> is free — claim it';if(res.reason==='reserved')return'misa.lol/<b>'+esc(name)+'</b> is reserved — try another';return'misa.lol/<b>'+esc(name)+'</b> is taken — try '+fixBtn(name+'_')+' or '+fixBtn(name.length<24?name+'2':name.slice(0,23)+'2');}
function fixBtn(v){return'<button type="button" class="claim__fix" data-fix="'+esc(v)+'" aria-label="Use '+esc(v)+'">'+esc(v)+'</button>';}
function cleanName(v){return v.trim().replace(/^https?:\/\//i,'').replace(/^(www\.)?misa\.lol\//i,'').replace(/^@/,'').replace(/[\/?#].*$/,'');}
function nameProblem(v){if(/\s/.test(v)){var u=v.replace(/\s+/g,'_');return RE.test(u)?'no spaces — use '+fixBtn(u.toLowerCase()):'no spaces — letters, numbers and underscores';}
if(/[^\x00-\x7f]/.test(v))return'letters, numbers and underscores only — no emoji or accents';if(!/^[A-Za-z]/.test(v))return'has to start with a letter';if(v.length<3)return'at least 3 characters';if(/[^A-Za-z0-9_]/.test(v)){var c=v.replace(/[^A-Za-z0-9_]/g,'');return'just letters, numbers and underscores'+(RE.test(c)?' — '+fixBtn(c.toLowerCase()):'');}
return'3–24 characters, starts with a letter, letters / numbers / underscores';}
var primary=$('#claim-name');var claimLive=null;function sayClaim(t){if(!claimLive){claimLive=document.createElement('p');claimLive.className='sr-only';claimLive.setAttribute('aria-live','polite');claimLive.setAttribute('data-claim-live','');document.body.appendChild(claimLive);}claimLive.textContent='';setTimeout(function(){claimLive.textContent=t;},30);}
function setInvalid(form,input,bad){form.classList.toggle('is-invalid',!!bad);if(bad)input.setAttribute('aria-invalid','true');else input.removeAttribute('aria-invalid');}
$$('form.claim').forEach(function(form){var input=$('.claim__input',form),hint=$('[data-hint]',form.parentElement),base=hint.textContent;form.noValidate=true;input.addEventListener('input',function(){var raw=input.value,v=cleanName(raw);if(v!==raw.trim()&&/misa\.lol\/|^@|^https?:/i.test(raw.trim())){input.value=v;}
if(input.hasAttribute('data-live'))setName(RE.test(v)?v:'');if(!v){hint.textContent=base;setInvalid(form,input,false);return;}
if(RE.test(v)){hint.innerHTML='misa.lol/<b>'+esc(v.toLowerCase())+'</b> — hit claim to check it’s free'+(v!==v.toLowerCase()?' <small>(links are lowercase)</small>':'');setInvalid(form,input,false);checkAvail(v.toLowerCase(),function(res){if(input.value.trim().toLowerCase()!==v.toLowerCase())return;var h=availHint(v,res);if(h){hint.innerHTML=h;setInvalid(form,input,res.available===false);sayClaim(hint.textContent.replace(/\s+/g,' ').trim());}});}
else{hint.innerHTML=nameProblem(v);setInvalid(form,input,true);}});hint.addEventListener('click',function(e){var b=e.target.closest('[data-fix]');if(!b)return;input.value=b.dataset.fix;input.dispatchEvent(new Event('input',{bubbles:true}));input.focus();});form.addEventListener('submit',function(e){var v=cleanName(input.value);input.value=v;if(v&&(!RE.test(v)||form.classList.contains('is-invalid'))){e.preventDefault();setInvalid(form,input,true);sayClaim(hint.textContent.replace(/\s+/g,' ').trim());input.focus();return;}
if(themePicked&&phone&&phone.dataset.theme){var h=$('input[name="theme"]',form);if(!h){h=document.createElement('input');h.type='hidden';h.name='theme';form.appendChild(h);}h.value=phone.dataset.theme;}});});if(window.visualViewport){var vvTimer=null;window.visualViewport.addEventListener('resize',function(){var el=document.activeElement;if(!el||!/^(INPUT|TEXTAREA|SELECT)$/.test(el.tagName))return;var wrap=el.closest('.claim, .field, .social-row, .search__box');if(!wrap)return;clearTimeout(vvTimer);vvTimer=setTimeout(function(){if(document.activeElement!==el)return;var vv=window.visualViewport,edge=vv.offsetTop+vv.height;var low=wrap.getBoundingClientRect().bottom;if(wrap.classList.contains('claim')){var hint=wrap.parentElement&&$('.claim__hint',wrap.parentElement);if(hint)low=Math.max(low,hint.getBoundingClientRect().bottom);}
if(wrap.classList.contains('search__box')){var cnt=$('#explore-count');if(cnt)low=Math.max(low,cnt.getBoundingClientRect().bottom);}
var bar=$('.dash__savebar');if(bar&&!bar.hidden){var br=bar.getBoundingClientRect();if(br.top<edge&&br.bottom>vv.offsetTop)edge=Math.min(edge,br.top);}
var need=Math.ceil(low+12-edge);if(need>0)window.scrollBy(0,need);},120);});}
$$('[data-claim]').forEach(function(a){a.addEventListener('click',function(){var v=primary?cleanName(primary.value):'',q=[];if(v&&RE.test(v))q.push('username='+encodeURIComponent(v));if(themePicked&&phone&&phone.dataset.theme)q.push('theme='+encodeURIComponent(phone.dataset.theme));var base=(a.getAttribute('href')||'/signup').split('?')[0];a.setAttribute('href',base+(q.length?'?'+q.join('&'):''));});});var oopsIn=$('[data-oops-path]');if(oopsIn&&ON_SITE){var parts=decodeURIComponent(location.pathname.replace(/^\/+|\/+$/g,'')).split('/'),seg=parts.length===1?parts[0]:'';if(seg&&seg!=='404'&&seg!=='404.html'&&RE.test(seg)){oopsIn.value=seg;oopsIn.dispatchEvent(new Event('input'));var lede=$('[data-oops-lede]');if(lede)lede.innerHTML='There’s no page at misa.lol/<b>'+esc(seg.toLowerCase())+'</b> — which means nobody has that name yet.';checkAvail(seg.toLowerCase(),function(res){if(lede&&res&&res.available===false)lede.innerHTML='There’s no page at misa.lol/<b>'+esc(seg.toLowerCase())+'</b> right now'+(res.reason==='reserved'?' — that name is reserved.':' — and that name isn’t free either.');});}}
var auth=$('.auth');if(auth){var q=new URLSearchParams(location.search);var EMAIL=/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/;function hintOf(f){return f?$('[data-field-hint]',f):null;}
function baseOf(f){var h=hintOf(f);if(h&&h.dataset.base==null)h.dataset.base=h.innerHTML;return h?h.dataset.base:'';}
function setField(f,state,msg){if(!f)return;f.classList.remove('is-valid','is-invalid');if(state)f.classList.add(state==='ok'?'is-valid':'is-invalid');var h=hintOf(f);if(h&&msg!=null)h.innerHTML=msg;var inp=$('input,select',f);if(inp){if(state==='bad')inp.setAttribute('aria-invalid','true');else inp.removeAttribute('aria-invalid');}}
function wire(inp,check){inp.__check=check;inp.addEventListener('input',function(){check(false);});inp.addEventListener('blur',function(){if(inp.value)check(true);});check(false);}
var user=$('[data-auth-user]',auth),label=$('[data-claim-label]',auth);if(user){var uf=user.closest('.field');baseOf(uf);var pre=(q.get('username')||'').trim();if(pre&&RE.test(pre))user.value=pre;wire(user,function(final){var v=user.value.trim(),ok=RE.test(v);setName(ok?v:'');if(label)label.textContent=ok?v:'yourname';if(!v){setField(uf,null,baseOf(uf));return!user.required;}
if(ok){if(availLast.name===v.toLowerCase()&&availLast.res&&availLast.res.available===false){setField(uf,'bad',availHint(v,availLast.res));return false;}
setField(uf,null,'misa.lol/<b>'+esc(v)+'</b> — we’ll check it’s free when you claim');checkAvail(v.toLowerCase(),function(res){if(user.value.trim().toLowerCase()!==v.toLowerCase())return;var h=availHint(v,res);if(h)setField(uf,res.available?'ok':'bad',h);});}
else setField(uf,'bad','3–24 characters, starts with a letter — letters, numbers, underscores');return ok;});if(pre&&RE.test(pre)&&matchMedia('(hover:hover)').matches)setTimeout(function(){var next=$('input[type="email"]',auth);if(next&&!next.value)next.focus();},50);}
if(THEMES.indexOf(q.get('theme'))>=0){setTheme(q.get('theme'));try{sessionStorage.setItem('misaTheme',q.get('theme'));}catch(e){}}
var plan=q.get('plan'),chip=$('[data-plan-chip]',auth),planInput=$('[data-plan-input]',auth);if(chip&&/^(lifetime|supporter)$/.test(plan||'')){chip.hidden=false;chip.classList.toggle('chip--gold',plan==='supporter');$('[data-plan-name]',chip).textContent=plan==='lifetime'?'lifetime · $5 once':'supporter · $9.99 once';if(planInput)planInput.value=plan;}
$$('input[type="email"]',auth).forEach(function(inp){var f=inp.closest('.field');baseOf(f);wire(inp,function(final){var v=inp.value.trim();if(!v){setField(f,final?'bad':null,final?'we need an email to recover your page':baseOf(f));return false;}var ok=EMAIL.test(v);if(ok)setField(f,'ok',baseOf(f));else setField(f,final?'bad':null,final?'that doesn’t look like an email':baseOf(f));return ok;});});$$('[data-pw]',auth).forEach(function(inp){var f=inp.closest('.field'),min=inp.minLength>0?inp.minLength:0;baseOf(f);wire(inp,function(final){var v=inp.value;if(!v){setField(f,final?'bad':null,final?'you’ll need one of these':baseOf(f));return false;}var ok=!min||v.length>=min;if(ok)setField(f,'ok',baseOf(f));else setField(f,final?'bad':null,final?(min+'+ characters, please'):baseOf(f));return ok;});});$$('[data-pw-confirm]',auth).forEach(function(inp){var f=inp.closest('.field'),src=document.getElementById(inp.getAttribute('data-pw-confirm'));baseOf(f);wire(inp,function(final){var v=inp.value;if(!v){setField(f,final?'bad':null,final?'type it again':baseOf(f));return false;}var ok=!!src&&v===src.value;if(ok)setField(f,'ok',baseOf(f));else setField(f,final?'bad':null,final?'these don’t match':baseOf(f));return ok;});if(src)src.addEventListener('input',function(){if(inp.value)inp.__check(false);});});$$('.check input[required]',auth).forEach(function(inp){var lab=inp.closest('.check');inp.__check=function(final){var ok=inp.checked;lab.classList.toggle('is-invalid',!ok&&final);return ok;};inp.addEventListener('change',function(){inp.__check(false);});});$$('input[required]',auth).forEach(function(inp){if(inp.__check)return;var f=inp.closest('.field');baseOf(f);wire(inp,function(final){var v=inp.value.trim();if(!v){setField(f,final?'bad':null,final?'this one’s required':baseOf(f));return false;}setField(f,null,baseOf(f));return true;});});$$('[data-eye]',auth).forEach(function(b){var inp=$('input',b.parentElement);b.addEventListener('click',function(){var show=inp.type==='password';inp.type=show?'text':'password';b.setAttribute('aria-pressed',String(show));b.setAttribute('aria-label',show?'Hide password':'Show password');$('use',b).setAttribute('href',show?'#i-eye-off':'#i-eye');inp.focus();});});$$('form',auth).forEach(function(form){form.addEventListener('submit',function(e){var bad=null;$$('input',form).forEach(function(inp){if(inp.__check&&!inp.__check(true)&&!bad)bad=inp;});if(bad){e.preventDefault();e.stopImmediatePropagation();bad.focus();}
var u=$('[data-auth-user]',form);if(u&&u.value)u.value=u.value.trim().toLowerCase();});});}
var loops=$$('video[data-src]');if(loops.length&&!reduce&&matchMedia('(min-width:941px)').matches&&!(navigator.connection&&navigator.connection.saveData)){var playLoop=function(v){if(v.src&&v.paused&&v.play)v.play().catch(function(){});};var webmOK=!!(document.createElement('video').canPlayType&&document.createElement('video').canPlayType('video/webm; codecs="vp9"'));var startLoops=function(){loops.forEach(function(av){if(av.src)return;av.addEventListener('canplay',function(){av.classList.add('is-on');},{once:true});av.src=(webmOK&&av.dataset.srcWebm)||av.dataset.src;playLoop(av);});};var kick=function(){if('requestIdleCallback'in window)requestIdleCallback(startLoops,{timeout:1500});else setTimeout(startLoops,250);};if(document.readyState==='complete')kick();else addEventListener('load',kick,{once:true});if('IntersectionObserver'in window){var vio=new IntersectionObserver(function(en){en.forEach(function(x){if(x.isIntersecting)playLoop(x.target);else x.target.pause();});},{threshold:.02});loops.forEach(function(v){vio.observe(v);});}
addEventListener('visibilitychange',function(){loops.forEach(function(v){if(document.hidden)v.pause();else if(v.getBoundingClientRect().bottom>0&&v.getBoundingClientRect().top<innerHeight)playLoop(v);});});}
var pvids=$$('video.phone__video[data-autoplay]');if(pvids.length){var startPhones=function(){pvids.forEach(function(v){if(v.dataset.poster&&!v.getAttribute('poster'))v.setAttribute('poster',v.dataset.poster);});if(reduce||(navigator.connection&&navigator.connection.saveData))return;pvids.forEach(function(v){var ph=v.closest('.phone');if(ph&&!ph.classList.contains('video-on'))return;v.play&&v.play().catch(function(){});});};if(document.readyState==='complete')startPhones();else addEventListener('load',startPhones,{once:true});}
var PAGES=[{theme:'gothic',name:'ren',handle:'ren',bio:'not a phase.',links:[['music','new single — graveyard shift'],['video','tiktok'],['users','discord server']],track:['graveyard shift','ren · 2:41'],views:'12.4k views',badge:'blue',tags:'music producer band',featured:true},{theme:'soft',name:'peach',handle:'peach',bio:'cute but unhinged ♡ streaming tue / thu / sun',links:[['heart','wishlist'],['video','twitch'],['image','art commissions (open!)']],track:['sugar rush!','peach · 3:05'],views:'48.1k views',badge:'blue',tags:'streamer twitch art commissions',featured:true},{theme:'mono',name:'void',handle:'void',bio:'less.',links:[['globe','portfolio'],['code','github'],['mail','email']],track:null,views:'891 views',badge:null,tags:'developer design portfolio'},{theme:'y2k',name:'n0t_found',handle:'n0t_found',bio:'> user not found_',links:[['music','spotify'],['users','server · 17 online'],['game','steam']],track:['hyperpop_final_v3.wav','n0t_found · 1:58'],views:'3,208 views',badge:'blue',tags:'hyperpop gamer music'},{theme:'sunset',name:'lune',handle:'lune',bio:'3am sets. bookings in bio.',links:[['music','mixes on soundcloud'],['cal','next show · sep 19'],['image','instagram']],track:['3am set (live)','lune · 58:12'],views:'6.7k views',badge:'gold',tags:'dj music club',featured:true},{theme:'paper',name:'kuro',handle:'kuro',bio:'a small press for small feelings.',links:[['bag','the zine shop'],['book','substack'],['video','letterboxd']],track:['rain on tin (field recording)','kuro · 6:20'],views:'1,104 views',badge:'blue',tags:'zines writing shop'},{theme:'vampire',name:'dorian',handle:'dorian',bio:'nocturnes for bass. nothing before midnight.',links:[['music','bandcamp'],['cal','the crypt tour'],['bag','shirts & tapes']],track:['nocturne no. 4','dorian · 5:03'],views:'4.2k views',badge:'blue',tags:'music bass nocturne goth'},{theme:'cathedral',name:'vesper',handle:'vesper',bio:'organ recitals in empty churches.',links:[['music','recordings'],['cal','next recital · sep 21'],['image','the photographs']],track:['evensong (live)','vesper · 12:40'],views:'2,860 views',badge:'gold',tags:'organ classical recital church',featured:true},{theme:'static',name:'noise',handle:'noise',bio:'field recordings of broken machines.',links:[['bag','the tape label'],['globe','the archive'],['mail','send me a machine']],track:['washing machine, 3am','noise · 8:12'],views:'1,540 views',badge:null,tags:'noise tapes field recordings experimental'},{theme:'lavender',name:'mira',handle:'mira',bio:'soft synth. softer opinions.',links:[['music','the playlist'],['heart','ko-fi'],['book','sunday newsletter']],track:['lilac hour','mira · 3:48'],views:'15.9k views',badge:'blue',tags:'synth music playlist newsletter',featured:true},{theme:'gothic',name:'mortis',handle:'mortis',bio:'bassist. cathedral acoustics only.',links:[['music','bandcamp'],['cal','tour dates'],['bag','merch']],track:['requiem for a landline','mortis · 4:12'],views:'9.3k views',badge:'blue',tags:'music band bass merch'},{theme:'soft',name:'mochi',handle:'mochi',bio:'vtuber (in training) · debut soon ♡',links:[['video','youtube'],['heart','ko-fi'],['users','fan discord']],track:['lullaby.exe','mochi · 2:20'],views:'21.7k views',badge:'gold',tags:'vtuber streamer youtube'},{theme:'mono',name:'hex',handle:'hex',bio:'i write things that break other things.',links:[['code','github'],['book','blog'],['mail','hire me']],track:null,views:'2,410 views',badge:null,tags:'developer code blog'},{theme:'y2k',name:'gl1tch',handle:'gl1tch',bio:'your favourite artist’s favourite artist',links:[['image','art archive'],['bag','prints'],['game','itch.io']],track:['dial-up dreams','gl1tch · 3:33'],views:'5.5k views',badge:'blue',tags:'artist pixel art prints games',featured:true},{theme:'sunset',name:'nova',handle:'nova',bio:'photos of places at the wrong time of day.',links:[['image','portfolio'],['cal','book a shoot'],['globe','prints & zines']],track:null,views:'7.9k views',badge:'blue',tags:'photographer photos prints'},{theme:'paper',name:'ivy',handle:'ivy',bio:'poems. tea. a small dog.',links:[['book','the newsletter'],['heart','buy me a tea'],['mail','say hi']],track:['reading: winter, again','ivy · 1:07'],views:'640 views',badge:null,tags:'poet writing newsletter'}];var THEME_SW={gothic:'linear-gradient(135deg,#F00646,#1a0006)',soft:'linear-gradient(135deg,#ffb3d1,#c9b6ff)',mono:'linear-gradient(135deg,#fff 50%,#000 50%)',y2k:'linear-gradient(135deg,#39ffa0,#1a5cff)',sunset:'linear-gradient(135deg,#ff8a3d,#7b2cff)',paper:'#EEE8E2',vampire:'linear-gradient(135deg,#b3001b,#0b0004)',cathedral:'linear-gradient(135deg,#d4a24c,#0e0c14)',static:'repeating-linear-gradient(0deg,#f5f5f5 0 2px,#101010 2px 4px)',lavender:'linear-gradient(135deg,#c9b6ff,#7c5cff)'};function ic(n){return'<svg class="ic" aria-hidden="true"><use href="#i-'+n+'"/></svg>';}
function phoneHTML(p){var badge=p.badge?'<span class="pf__badge'+(p.badge==='gold'?' pf__badge--gold':'')+'">'+ic('check')+'</span>':'';var links=p.links.map(function(l){return'<span class="pf__link">'+ic(l[0])+'<span class="pf__label">'+esc(l[1])+'</span>'+ic('arrow')+'</span>';}).join('');var music=p.track?'<div class="pf__music"><span class="pf__art">'+ic('play')+'</span><span class="pf__track"><b>'+esc(p.track[0])+'</b><span>'+esc(p.track[1])+'</span><span class="pf__bar"><i></i></span></span></div>':'';return'<div class="phone phone--sm theme-'+p.theme+'" aria-hidden="true"><div class="phone__notch"></div><div class="phone__bg"></div><div class="phone__art"></div><div class="phone__glare"></div>'+'<div class="pf"><div class="pf__avatar">'+esc(p.name.charAt(0))+'</div><div class="pf__name"><span>'+esc(p.name)+'</span>'+badge+'</div><div class="pf__handle">misa.lol/'+esc(p.handle)+'</div><p class="pf__bio">'+esc(p.bio)+'</p>'+'<div class="pf__links">'+links+'</div>'+music+'<div class="pf__foot"><span class="pf__views">'+ic('eye')+esc(p.views)+'</span></div></div></div>';}
function capHTML(p){return'<span class="film__cap hand"><span>'+esc(p.name)+'\u2019s page</span> <small>'+esc(p.theme)+'</small></span>';}
var film=$('#film');if(film){film.innerHTML=PAGES.slice(0,6).map(function(p){return'<button type="button" class="film__item reveal" data-set-theme="'+p.theme+'" aria-label="Try the '+p.theme+' theme">'+phoneHTML(p)+capHTML(p)+'</button>';}).join('');$$('.film__item',film).forEach(function(b){b.addEventListener('click',function(){setTheme(b.dataset.setTheme);var h=$('.hero');if(h)h.scrollIntoView({behavior:reduce?'auto':'smooth',block:'start'});});});}
var gallery=$('#gallery');if(gallery){function startLink(p){return'/signup?theme='+p.theme;}
var feat=$('#featured-film');if(feat)feat.innerHTML=PAGES.filter(function(p){return p.featured;}).map(function(p){return'<a class="film__item reveal" href="'+startLink(p)+'" aria-label="Start from '+esc(p.name)+'’s page ('+p.theme+' theme)">'+phoneHTML(p)+capHTML(p)+'</a>';}).join('');gallery.innerHTML=PAGES.map(function(p){var hay=(p.name+' '+p.handle+' '+p.bio+' '+p.links.map(function(l){return l[1];}).join(' ')+' '+(p.tags||'')+' '+p.theme).toLowerCase();return'<article class="card reveal" data-theme="'+p.theme+'" data-search="'+esc(hay)+'">'+'<a class="card__phone" href="'+startLink(p)+'" aria-label="Start from '+esc(p.name)+'’s page ('+p.theme+' theme)">'+phoneHTML(p)+'</a>'+'<div class="card__meta">'+capHTML(p)+'<a class="card__cta" href="'+startLink(p)+'" aria-label="Start from '+esc(p.name)+'’s page — '+p.theme+' theme">start from this theme →</a></div></article>';}).join('');var filters=$('#filters'),curTheme='all';filters.innerHTML=['all'].concat(THEMES).map(function(t){return'<button class="filter" type="button" data-filter="'+t+'" aria-pressed="'+(t==='all')+'"'+(t==='all'?'':' style="--sw:'+THEME_SW[t]+'"')+'>'+(t==='all'?'':'<i aria-hidden="true"></i>')+t+'</button>';}).join('');var qbox=$('#explore-q'),countEl=$('#explore-count'),emptyEl=$('#explore-empty'),cards=$$('.card',gallery);function setFilter(t){curTheme=t;$$('.filter',filters).forEach(function(x){x.setAttribute('aria-pressed',String(x.dataset.filter===t));});}
function applyFilters(){var s=(qbox.value||'').trim().toLowerCase(),n=0;cards.forEach(function(c){var ok=(curTheme==='all'||c.dataset.theme===curTheme)&&(!s||c.dataset.search.indexOf(s)>=0);c.classList.toggle('is-hidden',!ok);if(ok){n++;c.classList.add('in');}});countEl.textContent=(n===cards.length?cards.length+' example pages':n+' of '+cards.length+' example pages')+(curTheme!=='all'?' · '+curTheme:'')+(s?' · “'+s+'”':'');emptyEl.hidden=n>0;}
$$('.filter',filters).forEach(function(b){b.addEventListener('click',function(){setFilter(b.dataset.filter);applyFilters();});});qbox.addEventListener('input',applyFilters);if(qbox.form)qbox.form.addEventListener('submit',function(e){e.preventDefault();applyFilters();if(window.matchMedia&&matchMedia('(hover:none)').matches)qbox.blur();});var qs=new URLSearchParams(location.search);if(THEMES.indexOf(qs.get('theme'))>=0)setFilter(qs.get('theme'));if(qs.get('q'))qbox.value=qs.get('q');applyFilters();}
var demoCap=$('[data-demo-capsule]');if(demoCap){var capWhen=Date.parse(demoCap.getAttribute('data-demo-capsule')+'T00:00:00Z');(function capDemo(){var d=capWhen-Date.now();if(d<=0){demoCap.textContent='opening now';return;}
var days=Math.floor(d/86400000),hrs=Math.floor(d/3600000)%24;demoCap.textContent=days>0?(days+(days===1?' day':' days')+', '+hrs+'h to go'):(hrs+'h to go');setTimeout(capDemo,60000);})();}
var wall=$('#wall');if(wall){var ROT=[-3,2,-1.5,3,1,-2.5,2.5,-1,1.5,-3,2,-2],YS=[0,18,-8,10,6,-14,0,12,-6,8,-10,4],STK=['not a phase','3am sets','open commissions','less.','user not found','tiny press','nothing before midnight','empty churches','broken machines','lilac hour','cathedral acoustics','debut soon','breaks things','pixel witch','wrong time of day','a small dog'];var wallSmall=matchMedia('(max-width:640px)');function renderWall(all){wall.innerHTML=PAGES.slice(0,all?PAGES.length:10).map(function(p,i){return'<button type="button" class="wall__item" data-set-theme="'+p.theme+'" style="--r:'+ROT[i%ROT.length]+'deg;--y:'+YS[i%YS.length]+'px;--sr:'+(ROT[(i+3)%ROT.length]*2)+'deg" aria-label="'+esc(p.name)+'’s page — try the '+p.theme+' theme">'+'<span class="wall__sticker" aria-hidden="true">'+esc(STK[i%STK.length])+'</span>'+phoneHTML(p)+capHTML(p)+'<span class="wall__try" aria-hidden="true">try this look</span></button>';}).join('');$$('.wall__item',wall).forEach(function(b){b.addEventListener('click',function(){themePicked=true;setTheme(b.dataset.setTheme);var sw=$('.swatch[data-set-theme="'+b.dataset.setTheme+'"]');if(sw)sw.focus({preventScroll:true});showPhone();});});}
var hitTimer=null;function showPhone(){var target=phone||$('.hero3');if(!target)return;target.scrollIntoView({behavior:reduce?'auto':'smooth',block:phone?'center':'start'});if(!phone||reduce)return;phone.classList.remove('is-hit');void phone.offsetWidth;phone.classList.add('is-hit');clearTimeout(hitTimer);hitTimer=setTimeout(function(){phone.classList.remove('is-hit');},1500);}
renderWall(!wallSmall.matches);if(wallSmall.addEventListener)wallSmall.addEventListener('change',function(m){if(!m.matches&&wall.children.length<PAGES.length)renderWall(true);});}
var tick=$('.ticker');if(tick&&'IntersectionObserver'in window){var tickSeen=true;function tickSync(){tick.classList.toggle('is-off',document.hidden||!tickSeen);}
new IntersectionObserver(function(en){tickSeen=en[0].isIntersecting;tickSync();},{threshold:0}).observe(tick);addEventListener('visibilitychange',tickSync);}
var tickBtn=tick&&$('[data-ticker-pause]',tick);if(tickBtn){var setTick=function(off){tick.classList.toggle('is-paused',off);tickBtn.setAttribute('aria-pressed',String(off));tickBtn.setAttribute('aria-label',off?'Play the ticker':'Pause the ticker');try{if(off)sessionStorage.setItem('misaTicker','off');else sessionStorage.removeItem('misaTicker');}catch(e){}};var tickOff=false;try{tickOff=sessionStorage.getItem('misaTicker')==='off';}catch(e){}setTick(tickOff);tickBtn.addEventListener('click',function(){setTick(!tick.classList.contains('is-paused'));});}
var halo=$('[data-halo]');if(halo&&!reduce&&matchMedia('(hover:hover) and (pointer:fine)').matches){var hx=-100,hy=-100,tx=-100,ty=-100,shown=false;addEventListener('pointermove',function(e){tx=e.clientX;ty=e.clientY;if(!shown){shown=true;halo.classList.add('is-on');}var t=e.target.closest&&e.target.closest('a,button,input,[role="button"]');halo.classList.toggle('is-hover',!!t);},{passive:true});addEventListener('pointerleave',function(){halo.classList.remove('is-on');shown=false;});(function tick(){hx+=(tx-hx)*.18;hy+=(ty-hy)*.18;halo.style.transform='translate('+hx+'px,'+hy+'px) translate(-50%,-50%)';requestAnimationFrame(tick);})();}
var dash=$('.dash-editor');if(dash){var PLATFORMS=['YouTube','Discord','Instagram','X','TikTok','Telegram','Spotify','SoundCloud','GitHub','Reddit','Twitch','Snapchat','Facebook','LinkedIn','Steam','Roblox','PayPal','Pinterest','Threads','Kick','Patreon','Bitcoin','Ethereum','Litecoin','Solana','Email','Custom URL'];var PLAT_ICON={YouTube:'video',TikTok:'video',Twitch:'video',Kick:'video',Spotify:'music',SoundCloud:'music',Discord:'users',Reddit:'users',Telegram:'mail',GitHub:'code',Steam:'game',Roblox:'game',Email:'mail',Instagram:'image',Pinterest:'image',Snapchat:'image',PayPal:'bag',Patreon:'heart',X:'globe',Threads:'globe',Facebook:'globe',LinkedIn:'globe'};function defaults(username){return{profile:{username:username||'',displayName:username||'',description:'',location:'',views:0,uid:''},settings:{accentColor:'#F00646',textColor:'#F2F2F0',backgroundColor:'#050606',iconColor:'#F00646',profileOpacity:.72,backgroundOpacity:.55,profileBlur:16,profileRadius:28,profileGradient:true,showViews:true,showBadges:true,showSocials:true,entryScreen:false,entryText:'click to enter',backgroundEffect:'None',usernameEffect:'None',usernameGlow:false,socialGlow:false,bioTypewriter:false,entryAnimation:'None',hideBrand:false,cursor:'default',supporterTag:false,sparkleTrail:false,goldBorder:false,font:'inter',playerStyle:'bars',linkStyle:'pill',avatarFrame:'none',tabTitle:'none',guestbook:false,doodles:false,since:false,replay:false,asks:false,remember:false,vigil:'',reverse:{title:'',text:''},tally:{q:'',options:[]},draw:'',night:{tz:'',from:23,to:5},archive:false,neighbours:[],moon:false,presence:false,counterStyle:'plain',mood:false,secret:{word:'',url:null,label:''},capsule:{text:'',at:''},sky:''},assets:{avatar:{url:null},background:{url:null},backgroundVideo:{url:null},audio:{url:null},cursor:{url:null},audioEnabled:true,volume:.6},socials:[],badges:[]};}
var cfg=defaults(''),me=null,dirty=false;function get(path){return path.split('.').reduce(function(o,k){return o==null?undefined:o[k];},cfg);}
function set(path,val){var ks=path.split('.'),o=cfg;ks.slice(0,-1).forEach(function(k,i){if(o[k]==null||typeof o[k]!=='object')o[k]=/^\d+$/.test(ks[i+1])?[]:{};o=o[k];});o[ks[ks.length-1]]=val;}
function fields(){return $$('[data-cfg]',dash);}
function readField(el){var t=el.type;if(t==='checkbox')return el.checked;if(t==='range'||t==='number')return parseFloat(el.value);if(t==='url'||t==='text'||el.tagName==='TEXTAREA'){var v=el.value.trim();return(t==='url'&&!v)?null:v;}return el.value;}
var PAGE_HOSTS=/(^|\.)(youtube\.com|youtu\.be|spotify\.com|soundcloud\.com|tiktok\.com|instagram\.com|x\.com|twitter\.com|facebook\.com|vimeo\.com|drive\.google\.com|pinterest\.com|threads\.net)$/i;var EXT={image:/\.(png|jpe?g|webp|gif|avif|svg)(\?|#|$)/i,audio:/\.(mp3|ogg|wav|m4a|aac|flac|opus)(\?|#|$)/i,video:/\.(mp4|webm|mov|m4v)(\?|#|$)/i};function assetDoubt(kind,v){v=String(v||'').trim();if(!v)return null;if(!/^https?:\/\//i.test(v)&&!/^\//.test(v))return'needs to start with https:// — the page will ignore this one';var host='';try{host=new URL(v,location.origin).hostname;}catch(e){return'that doesn’t look like a link';}
if(PAGE_HOSTS.test(host))return'that’s a page, not a file — the page needs a direct link to the '+kind+' (or an upload)';for(var k in EXT){if(k!==kind&&EXT[k].test(v))return'that looks like '+(k==='video'?'a video':'an '+k)+' file, not '+(kind==='image'?'an image':kind==='audio'?'audio':'a video');}
return null;}
function assetCheck(el){var kind=el.dataset.asset;if(!kind)return;var f=el.closest('.field'),note=f&&$('[data-asset-note]',f);if(!note)return;var d=assetDoubt(kind,el.value);note.hidden=!d;$('span:last-child',note).textContent=d||'';f.classList.toggle('is-invalid',!!d);f.classList.remove('is-valid');}
function fmtRange(el){return(+el.max<=1)?Math.round(el.value*100)+'%':Math.round(el.value)+'px';}
function readout(el){var code=el.parentElement&&$('[data-color-code]',el.parentElement);if(code)code.textContent=el.value;var out=el.type==='range'&&el.parentElement&&$('[data-range-out]',el.parentElement);if(out){out.textContent=fmtRange(el);el.setAttribute('aria-valuetext',fmtRange(el));}if(el.dataset.asset)assetCheck(el);}
function writeField(el,v){var t=el.type;if(t==='checkbox')el.checked=!!v;else if(t==='color')el.value=(typeof v==='string'&&/^#[0-9a-f]{6}$/i.test(v))?v:'#000000';else el.value=(v==null?'':v);readout(el);}
function hydrate(){fields().forEach(function(el){writeField(el,get(el.dataset.cfg));});renderSocials();renderBlocks();preview();stats();}
fields().forEach(function(el){el.addEventListener('input',function(){set(el.dataset.cfg,readField(el));readout(el);touch();});});function touch(){dirty=true;if(sbar&&sbar.classList.contains('is-saved'))saveState(null);$('[data-savebar]').hidden=false;preview();stats();checklist();syncTheme();stash();if(published)refreshStatus();}
var list=$('[data-socials]',dash);var HOSTS=[['youtube.com','YouTube'],['youtu.be','YouTube'],['discord.gg','Discord'],['discord.com','Discord'],['instagram.com','Instagram'],['x.com','X'],['twitter.com','X'],['tiktok.com','TikTok'],['t.me','Telegram'],['telegram.me','Telegram'],['spotify.com','Spotify'],['soundcloud.com','SoundCloud'],['github.com','GitHub'],['reddit.com','Reddit'],['twitch.tv','Twitch'],['snapchat.com','Snapchat'],['facebook.com','Facebook'],['fb.com','Facebook'],['linkedin.com','LinkedIn'],['steamcommunity.com','Steam'],['steampowered.com','Steam'],['roblox.com','Roblox'],['paypal.me','PayPal'],['paypal.com','PayPal'],['pinterest.com','Pinterest'],['threads.net','Threads'],['kick.com','Kick'],['patreon.com','Patreon']];function guessPlatform(v){v=String(v||'').trim().toLowerCase();if(!v)return null;if(/^mailto:/.test(v)||/^[^\s\/@]+@[^\s\/@]+\.[a-z]{2,}$/.test(v))return'Email';var host=v.replace(/^[a-z]+:\/\//,'').replace(/^www\./,'').split(/[\/?#]/)[0];if(!host)return null;for(var i=0;i<HOSTS.length;i++){var h=HOSTS[i][0];if(host===h||host.slice(-h.length-1)==='.'+h)return HOSTS[i][1];}
return null;}
var AUTO={};function autoPlatform(sc,row,key){var id=sc.id||('i'+row.dataset.i);if(key==='platform'){delete AUTO[id];return;}
if(key!=='value')return;var mine=AUTO[id]===sc.platform;if(!(sc.platform==='Custom URL'||mine))return;var g=guessPlatform(sc.value)||(mine?'Custom URL':null);if(!g||g===sc.platform)return;sc.platform=g;if(g==='Custom URL')delete AUTO[id];else AUTO[id]=g;var sel=$('[data-s="platform"]',row);if(!sel)return;sel.value=g;var box=sel.parentElement;if(box&&!reduce){box.classList.remove('is-auto');void box.offsetWidth;box.classList.add('is-auto');}
if(g!=='Custom URL')sayLinks('Platform set to '+g+'.');}
function rowName(sc,i){var t=String(sc.label||'').trim()||String(sc.value||'').trim()||(sc.platform&&sc.platform!=='Custom URL'?sc.platform:'empty');return'Link '+(i+1)+' — '+t;}
var linksLive=$('[data-links-live]',dash);function sayLinks(t){if(!linksLive)return;linksLive.textContent='';setTimeout(function(){linksLive.textContent=t;},30);}
function socialRow(sc,i){var opts=PLATFORMS.map(function(p){return'<option'+(p===sc.platform?' selected':'')+'>'+p+'</option>';}).join('');var empty=sc.enabled!==false&&!String(sc.value||'').trim();return'<div class="social-row'+(empty?' is-empty':'')+'" data-i="'+i+'" role="group" aria-label="'+esc(rowName(sc,i))+'">'+'<div class="field"><div class="field__box"><select class="field__input" data-s="platform" aria-label="Platform">'+opts+'</select></div></div>'+'<div class="field"><div class="field__box"><input class="field__input" type="text" maxlength="60" placeholder="label" value="'+esc(sc.label||'')+'" data-s="label" enterkeyhint="done" aria-label="Label"></div></div>'+'<div class="field"><div class="field__box"><input class="field__input" type="text" maxlength="200" placeholder="link or handle" value="'+esc(sc.value||'')+'" data-s="value" enterkeyhint="done" aria-label="Link"></div></div>'+'<label class="social-row__mode"><input type="checkbox" data-s="text"'+(sc.displayMode==='text'?' checked':'')+'> text</label>'+'<label class="toggle" data-toggle title="Show on page"><input type="checkbox" data-s="enabled"'+(sc.enabled!==false?' checked':'')+'><i aria-hidden="true"></i>show</label>'+'<span class="social-row__move"><button type="button" aria-label="Move up" data-move="-1"'+(i===0?' disabled':'')+'>'+ic('down')+'</button><button type="button" aria-label="Move down" data-move="1"'+(i===(cfg.socials.length-1)?' disabled':'')+'>'+ic('down')+'</button></span>'+'<button class="social-row__del" type="button" aria-label="Remove link" data-del>'+ic('plus')+'</button>'+'<p class="social-row__warn">won’t show on your page until it has a link or handle</p></div>';}
function renderSocials(){list.innerHTML=(cfg.socials||[]).map(socialRow).join('')||'<p class="dash__previewnote" style="text-align:left">no links yet — add your first one.</p>';$$('.social-row',list).forEach(function(row){var sc=cfg.socials[+row.dataset.i];$$('[data-s]',row).forEach(function(el){el.addEventListener('input',function(){var k=el.dataset.s;if(k==='text')sc.displayMode=el.checked?'text':'link';else if(k==='enabled')sc.enabled=el.checked;else sc[k]=el.value;autoPlatform(sc,row,k);row.classList.toggle('is-empty',sc.enabled!==false&&!String(sc.value||'').trim());if(k==='label'||k==='value'||k==='platform')row.setAttribute('aria-label',rowName(sc,+row.dataset.i));touch();});});$('[data-del]',row).addEventListener('click',function(){var i=+row.dataset.i,gone=cfg.socials[i];cfg.socials.splice(i,1);renderSocials();touch();var n=cfg.socials.length;sayLinks('Removed '+rowName(gone,i).replace(/^Link \d+ — /,'“')+'” — '+n+(n===1?' link':' links')+' left.');var next=$('.social-row[data-i="'+Math.min(i,n-1)+'"] [data-del]',list)||$('[data-add-social]',dash);if(next)next.focus();});$$('[data-move]',row).forEach(function(b){b.addEventListener('click',function(){var i=+row.dataset.i,j=i+(+b.dataset.move);if(j<0||j>=cfg.socials.length)return;var t=cfg.socials[i];cfg.socials[i]=cfg.socials[j];cfg.socials[j]=t;renderSocials();touch();var nb=$('.social-row[data-i="'+j+'"] [data-move="'+b.dataset.move+'"]',list);if(!nb||nb.disabled)nb=$$('.social-row[data-i="'+j+'"] [data-move]',list).filter(function(x){return!x.disabled;})[0];if(nb)nb.focus();sayLinks('Moved “'+rowName(t,j).replace(/^Link \d+ — /,'')+'” '+(j<i?'up':'down')+' to '+(j+1)+' of '+cfg.socials.length+'.');});});});}
$('[data-add-social]',dash).addEventListener('click',function(){cfg.socials=cfg.socials||[];cfg.socials.push({id:'s'+Date.now().toString(36),platform:'Custom URL',label:'',value:'',enabled:true,displayMode:'link',clicks:0});renderSocials();touch();var last=$$('.social-row input[data-s="label"]',list).pop();if(last)last.focus();sayLinks('Added link '+cfg.socials.length+' of '+cfg.socials.length+' — give it a label and a link.');});var lcBtn=$('[data-check-links]',dash),lcBox=$('[data-linkcheck]',dash);if(lcBtn&&lcBox){lcBtn.addEventListener('click',function(){if(lcBtn.getAttribute('aria-busy')==='true')return;if(dirty){formMsg($('#links',dash),'Save first — the sweep checks the links on your saved page.','error');return;}var sum=$('[data-linkcheck-sum]',lcBox),ul=$('[data-linkcheck-list]',lcBox);lcBtn.setAttribute('aria-busy','true');lcBtn.textContent='Checking…';lcBox.hidden=false;sum.textContent='Knocking on every link…';ul.innerHTML='';(DEMO?Promise.resolve({ok:true,json:function(){return Promise.resolve(DEMO['/api/v1/me/links/check']||{checked:0,results:[]});}}):fetch('/api/v1/me/links/check',{method:'POST',credentials:'include',headers:{Accept:'application/json'}})).then(function(r){return r.json().then(function(d){if(!r.ok){sum.textContent=r.status===429?'Three sweeps every ten minutes is the limit — give it a moment.':(typeof d.detail==='string'?d.detail:'Could not check right now.');return;}
var res=d.results||[],bad=res.filter(function(x){return!x.ok;});sum.textContent=!res.length?'No web links to check — email and text-only rows are skipped.':bad.length?(res.length+' checked · '+bad.length+(bad.length===1?' needs a look':' need a look')):(res.length+' checked · all fine');var byId={};(cfg.socials||[]).forEach(function(sc,i){byId[sc.id]=i;});ul.innerHTML=res.map(function(x){var i=byId[x.id],sc=cfg.socials&&cfg.socials[i],name=sc?(String(sc.label||'').trim()||sc.platform||'link'):'link';return'<li class="linkcheck__row'+(x.ok?'':' is-bad')+'"><b>'+esc(name)+'</b><span>'+esc(x.url.replace(/^https?:\/\//,'').slice(0,48))+'</span><em>'+esc(x.note)+'</em></li>';}).join('');res.forEach(function(x){var i=byId[x.id];var row=i!=null&&$('.social-row[data-i="'+i+'"]',list);if(row)row.classList.toggle('is-dead',!x.ok);});sayLinks(sum.textContent);});}).catch(function(){sum.textContent='Could not reach the server.';}).then(function(){lcBtn.removeAttribute('aria-busy');lcBtn.textContent='Check my links';});});}
var blist=$('[data-blocks]',dash),blocksLive=$('[data-blocks-live]',dash);var BLOCK_TYPES=[['heading','Heading'],['text','Paragraph'],['quote','Quote'],['divider','† divider'],['status','Currently…'],['clock','Clock — it’s 3:12 am in…'],['nowplaying','Now playing'],['weather','Weather where you are']];var myZone=(function(){try{return Intl.DateTimeFormat().resolvedOptions().timeZone||'';}catch(e){return'';}})();function zoneTime(tz){try{return new Intl.DateTimeFormat('en-US',{hour:'numeric',minute:'2-digit',timeZone:tz}).format(new Date()).toLowerCase();}catch(e){return'';}}
function zoneLabel(tz){return String(tz||'').split('/').pop().replace(/_/g,' ').toLowerCase();}
function blocks(){var st=cfg.settings=cfg.settings||{};if(!Array.isArray(st.blocks))st.blocks=[];return st.blocks;}
function sayBlocks(t){if(!blocksLive)return;blocksLive.textContent='';setTimeout(function(){blocksLive.textContent=t;},30);}
function blockName(b,i){var t=b.type==='divider'?'divider':b.type==='clock'?('clock · '+(String(b.text||'').trim()||zoneLabel(b.tz)||'no zone')):b.type==='nowplaying'?('now playing · '+(String(b.text||'').trim()||(b.lb?'live from '+b.lb:'nothing yet'))):b.type==='weather'?('weather · '+(String(b.city||'').trim()||'no place yet')):(String(b.text||'').trim()||'empty '+b.type);return'Block '+(i+1)+' — '+t.slice(0,40);}
function blockRow(b,i){var multi=b.type==='text'||b.type==='quote',n=blocks().length;return'<div class="social-row block-row'+(b.type==='divider'?' block-row--bare':b.type==='nowplaying'||b.type==='weather'?' block-row--np':'')+'" data-i="'+i+'" role="group" aria-label="'+esc(blockName(b,i))+'">'+'<div class="field"><div class="field__box"><select class="field__input" data-b="type" aria-label="Block type">'+BLOCK_TYPES.map(function(t){return'<option value="'+t[0]+'"'+(b.type===t[0]?' selected':'')+'>'+t[1]+'</option>';}).join('')+'</select></div></div>'+
(b.type==='divider'?'<div class="field block-row__note"><span class="dash__previewnote">a hairline with a † in the middle</span></div>':b.type==='weather'?'<div class="field block-row__text block-row__np"><div class="field__box"><input class="field__input" type="text" maxlength="64" placeholder="Berlin" value="'+esc(b.city||'')+'" data-b="city" autocomplete="off" aria-label="Place (a city)"></div><div class="field__box"><select class="field__input" data-b="unit" aria-label="Degrees"><option value="c"'+(b.unit!=='f'?' selected':'')+'>°C</option><option value="f"'+(b.unit==='f'?' selected':'')+'>°F</option></select></div><div class="field__box"><input class="field__input" type="text" maxlength="40" placeholder="what to call it (optional)" value="'+esc(b.text||'')+'" data-b="text" enterkeyhint="done" aria-label="Place name as it should read (optional)"></div><p class="field__hint"><span class="dot" aria-hidden="true"></span><span>reads “raining in berlin, 11°” — the weather is looked up on the live page, from Open-Meteo, no keys</span></p></div>':b.type==='nowplaying'?'<div class="field block-row__text block-row__np"><div class="field__box"><input class="field__input" type="text" maxlength="160" placeholder="graveyard shift — ren" value="'+esc(b.text||'')+'" data-b="text" enterkeyhint="done" aria-label="Track — artist"></div><div class="field__box"><input class="field__input" type="url" placeholder="https://…/the-track" value="'+esc(b.url||'')+'" data-b="url" aria-label="Link (optional)"></div><div class="field__box"><input class="field__input" type="text" maxlength="64" placeholder="ListenBrainz name (optional)" value="'+esc(b.lb||'')+'" data-b="lb" autocomplete="off" spellcheck="false" aria-label="ListenBrainz username (optional)"></div><p class="field__hint"><span class="dot" aria-hidden="true"></span><span>type what you’re playing — or give a ListenBrainz name and the page keeps it live, a spinning record and all</span></p></div>':b.type==='clock'?'<div class="field block-row__text block-row__clock"><div class="field__box"><input class="field__input" type="text" list="tz-list" maxlength="64" placeholder="Europe/Berlin" value="'+esc(b.tz||'')+'" data-b="tz" autocomplete="off" spellcheck="false" aria-label="Time zone (IANA name, e.g. Europe/Berlin)"></div><div class="field__box"><input class="field__input" type="text" maxlength="40" placeholder="'+esc(zoneLabel(b.tz)||'berlin')+'" value="'+esc(b.text||'')+'" data-b="text" enterkeyhint="done" aria-label="Place name, as it should read"></div><p class="field__hint"><span class="dot" aria-hidden="true"></span><span data-clock-hint>'+(zoneTime(b.tz)?'reads “it’s '+esc(zoneTime(b.tz))+' in '+esc(String(b.text||'').trim()||zoneLabel(b.tz))+'” right now':'pick a zone from the list — it needs an IANA name')+'</span></p></div>':'<div class="field block-row__text"><div class="field__box">'+(multi?'<textarea class="field__input" rows="2" maxlength="400" placeholder="'+(b.type==='quote'?'less, but louder.':'a few lines about anything')+'" data-b="text" aria-label="Text">'+esc(b.text||'')+'</textarea>':'<input class="field__input" type="text" maxlength="120" placeholder="'+(b.type==='status'?'recording the EP':'shows')+'" value="'+esc(b.text||'')+'" data-b="text" enterkeyhint="done" aria-label="Text">')+'</div></div>')+'<div class="field"><div class="field__box"><select class="field__input" data-b="place" aria-label="Where it sits"><option value="bottom"'+(b.place!=='top'&&b.place!=='back'?' selected':'')+'>below the links</option><option value="top"'+(b.place==='top'?' selected':'')+'>above the links</option><option value="back"'+(b.place==='back'?' selected':'')+'>on the back of the page</option></select></div></div>'+'<label class="toggle block-row__night" data-toggle title="Only on your page during your night — by day it isn’t in the page at all"><input type="checkbox" data-b="night"'+(b.night?' checked':'')+'><i aria-hidden="true"></i>night only</label>'+'<label class="toggle" data-toggle title="Show on page"><input type="checkbox" data-b="enabled"'+(b.enabled!==false?' checked':'')+'><i aria-hidden="true"></i>show</label>'+'<span class="social-row__move"><button type="button" aria-label="Move up" data-bmove="-1"'+(i===0?' disabled':'')+'>'+ic('down')+'</button><button type="button" aria-label="Move down" data-bmove="1"'+(i===n-1?' disabled':'')+'>'+ic('down')+'</button></span>'+'<button class="social-row__del" type="button" aria-label="Remove block" data-bdel>'+ic('plus')+'</button></div>';}
function renderBlocks(){if(!blist)return;var bl=blocks();blist.innerHTML=bl.map(blockRow).join('')||'<p class="dash__previewnote" style="text-align:left">no blocks yet — links are all most pages need; a “currently:” line is the one people notice.</p>';$$('.block-row',blist).forEach(function(row){var b=bl[+row.dataset.i];$$('[data-b]',row).forEach(function(el){el.addEventListener(el.dataset.b==='type'||el.dataset.b==='place'||el.dataset.b==='unit'||el.dataset.b==='night'?'change':'input',function(){var k=el.dataset.b;if(k==='enabled')b.enabled=el.checked;else if(k==='night')b.night=el.checked;else b[k]=el.value;if(b.type==='clock'){var h=$('[data-clock-hint]',row),zt=zoneTime(b.tz);if(h)h.textContent=zt?'reads “it’s '+zt+' in '+(String(b.text||'').trim()||zoneLabel(b.tz))+'” right now':'pick a zone from the list — it needs an IANA name';}if(k==='type'){if(b.type==='clock'&&!b.tz)b.tz=myZone;renderBlocks();var f=$('.block-row[data-i="'+row.dataset.i+'"] [data-b="text"]',blist)||$('.block-row[data-i="'+row.dataset.i+'"] [data-b="type"]',blist);if(f)f.focus();}row.setAttribute('aria-label',blockName(b,+row.dataset.i));touch();});});$('[data-bdel]',row).addEventListener('click',function(){var i=+row.dataset.i,gone=bl[i];bl.splice(i,1);renderBlocks();touch();sayBlocks('Removed '+blockName(gone,i).replace(/^Block \d+ — /,'“')+'” — '+bl.length+(bl.length===1?' block':' blocks')+' left.');var next=$('.block-row[data-i="'+Math.min(i,bl.length-1)+'"] [data-bdel]',blist)||$('[data-add-block]',dash);if(next)next.focus();});$$('[data-bmove]',row).forEach(function(btn){btn.addEventListener('click',function(){var i=+row.dataset.i,j=i+(+btn.dataset.bmove);if(j<0||j>=bl.length)return;var t=bl[i];bl[i]=bl[j];bl[j]=t;renderBlocks();touch();var nb=$('.block-row[data-i="'+j+'"] [data-bmove="'+btn.dataset.bmove+'"]',blist);if(!nb||nb.disabled)nb=$$('.block-row[data-i="'+j+'"] [data-bmove]',blist).filter(function(x){return!x.disabled;})[0];if(nb)nb.focus();sayBlocks('Moved “'+blockName(t,j).replace(/^Block \d+ — /,'')+'” '+(j<i?'up':'down')+' to '+(j+1)+' of '+bl.length+'.');});});});}
var addBlock=$('[data-add-block]',dash);if(addBlock)addBlock.addEventListener('click',function(){if(addBlock.disabled)return;var bl=blocks();if(bl.length>=12){sayBlocks('Twelve blocks is the most a page takes.');return;}bl.push({id:'b'+Date.now().toString(36),type:bl.length?'text':'status',text:'',place:'bottom',enabled:true});renderBlocks();touch();var last=$$('.block-row [data-b="text"]',blist).pop();if(last)last.focus();sayBlocks('Added block '+bl.length+' of '+bl.length+'.');});var pv=$('#dash-phone');var pvTypeTimer=null;function pvType(el,text){if(pvTypeTimer)clearTimeout(pvTypeTimer);var i=0;el.textContent='';(function step(){el.textContent=text.slice(0,++i);if(i<text.length)pvTypeTimer=setTimeout(step,text.charAt(i-1)==='.'?220:34);})();}
function cursorSvg(body){var svg='<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">'+body+'</svg>';return'data:image/svg+xml,'+svg.replace(/#/g,'%23').replace(/"/g,"'").replace(/</g,'%3C').replace(/>/g,'%3E');}
var PAGE_FONTS={inter:['',"'Inter',system-ui,-apple-system,'Segoe UI',Roboto,sans-serif",1],playfair:['Playfair+Display:ital,wght@0,400;0,700;0,800;1,400',"'Playfair Display',Georgia,serif",1.06],cormorant:['Cormorant+Garamond:ital,wght@0,500;0,700;1,500',"'Cormorant Garamond',Georgia,serif",1.12],'space-mono':['Space+Mono:wght@400;700',"'Space Mono',ui-monospace,Menlo,Consolas,monospace",.94],caveat:['Caveat:wght@500;700',"'Caveat','Segoe Print',cursive",1.22],vt323:['VT323',"'VT323',ui-monospace,monospace",1.24],bebas:['Bebas+Neue',"'Bebas Neue',Impact,sans-serif",1.08],unifraktur:['UnifrakturMaguntia',"'UnifrakturMaguntia','Old English Text MT',serif",1.06]};var fontsLoaded={};function ensureFont(key,query){if(!query||fontsLoaded[key]||DEMO&&DEMO.offline)return;fontsLoaded[key]=1;var l=document.createElement('link');l.rel='stylesheet';l.href='https://fonts.googleapis.com/css2?family='+query+'&display=swap';document.head.appendChild(l);}
var CURSORS={star:cursorSvg('<path d="M12 2l2.6 6.9L22 12l-7.4 3.1L12 22l-2.6-6.9L2 12l7.4-3.1z" fill="#E6C36B" stroke="#1a1408" stroke-width="1.4" stroke-linejoin="round"/>'),ring:cursorSvg('<circle cx="12" cy="12" r="8" fill="none" stroke="#E6C36B" stroke-width="3"/><circle cx="12" cy="12" r="8" fill="none" stroke="#1a1408" stroke-width="1" opacity=".6"/><circle cx="12" cy="12" r="1.6" fill="#E6C36B"/>'),snow:cursorSvg('<g fill="none" stroke="#E6C36B" stroke-width="2" stroke-linecap="round"><path d="M12 2v20M2 12h20M4.9 4.9l14.2 14.2M19.1 4.9L4.9 19.1"/><path d="M12 2l-2.5 2.5M12 2l2.5 2.5M12 22l-2.5-2.5M12 22l2.5-2.5M2 12l2.5-2.5M2 12l2.5 2.5M22 12l-2.5-2.5M22 12l-2.5 2.5"/></g>')};function preview(){if(!pv)return;var p=cfg.profile||{},st=cfg.settings||{},as=cfg.assets||{};pv.style.setProperty('--pf-acc',st.accentColor||'#F00646');pv.style.setProperty('--pf-bg',st.backgroundColor||'#050606');pv.style.setProperty('--pf-fg',st.textColor||'#F2F2F0');pv.style.setProperty('--pf-r',(st.profileRadius!=null?Math.min(st.profileRadius,22):14)+'px');pv.style.setProperty('--pf-av','linear-gradient(135deg,'+(st.accentColor||'#F00646')+',rgba(0,0,0,.6))');pv.style.setProperty('--pf-mute','color-mix(in srgb,'+(st.textColor||'#F2F2F0')+' 62%,transparent)');pv.style.setProperty('--pf-line','color-mix(in srgb,'+(st.textColor||'#F2F2F0')+' 12%,transparent)');var name=p.displayName||me&&me.username||'yourname';$('[data-pv-name]',pv).textContent=name;$('[data-pv-handle]',pv).textContent=(me&&me.username)||'yourname';var av=$('[data-pv-avatar]',pv);var url=as.avatar&&as.avatar.url;av.textContent=url?'':name.charAt(0).toLowerCase();av.style.backgroundImage=url?'url("'+url.replace(/["\\]/g,'')+'")':'';var okImg=function(u){return typeof u==='string'&&/^(https?:\/\/|\/)/.test(u)&&!assetDoubt('image',u);},okVid=function(u){return typeof u==='string'&&/^(https?:\/\/|\/)/.test(u)&&!assetDoubt('video',u);};var bgEl=$('.phone__bg',pv),bgUrl=as.background&&as.background.url;if(bgEl){bgEl.style.backgroundImage=okImg(bgUrl)?'url("'+bgUrl.replace(/["\\]/g,'')+'")':'';bgEl.style.backgroundSize='cover';bgEl.style.backgroundPosition='center';bgEl.style.opacity=okImg(bgUrl)?String(st.backgroundOpacity!=null?st.backgroundOpacity:1):'';}
var vUrl=as.backgroundVideo&&as.backgroundVideo.url,vEl=$('.phone__video',pv);if(okVid(vUrl)){if(!vEl){vEl=document.createElement('video');vEl.className='phone__video';vEl.muted=true;vEl.loop=true;vEl.playsInline=true;vEl.setAttribute('aria-hidden','true');vEl.preload='metadata';pv.insertBefore(vEl,$('.phone__glare',pv));}
if(vEl.getAttribute('src')!==vUrl){vEl.src=vUrl;}vEl.style.opacity=String(st.backgroundOpacity!=null?st.backgroundOpacity:1);if(!reduce)vEl.play&&vEl.play().catch(function(){});else vEl.pause&&vEl.pause();}
else if(vEl){vEl.pause&&vEl.pause();vEl.removeAttribute('src');vEl.load&&vEl.load();vEl.style.opacity='0';}
var bioEl=$('[data-pv-bio]',pv),bioText=p.description||'your bio.',typing=!!st.bioTypewriter&&!reduce;if(typing&&(bioEl.dataset.typed!==bioText||bioEl.dataset.mode!=='type')&&document.activeElement!==$('#f-bio')){bioEl.dataset.mode='type';bioEl.dataset.typed=bioText;pvType(bioEl,bioText);}
else if(!typing){if(bioEl.dataset.mode==='type'){delete bioEl.dataset.mode;delete bioEl.dataset.typed;if(pvTypeTimer)clearTimeout(pvTypeTimer);}bioEl.textContent=bioText;}
else if(document.activeElement===$('#f-bio')){bioEl.dataset.typed=bioText;if(pvTypeTimer)clearTimeout(pvTypeTimer);bioEl.textContent=bioText;}
var card=$('.pf',pv),anim=String(st.entryAnimation||'None').toLowerCase();if(card&&card.dataset.anim!==anim){card.dataset.anim=anim;card.className='pf';if(anim!=='none'&&!reduce){void card.offsetWidth;card.classList.add('in-'+anim);}}
var lk=$('[data-pv-links]',pv);if(lk)lk.className='pf__links pf__links--'+(String(st.linkStyle||'pill').toLowerCase());var avw=$('[data-pv-avatar]',pv);if(avw)avw.className='pf__avatar'+({ring:' frame--ring',halo:' frame--halo',pixel:' frame--pixel',petals:' frame--petals'}[String(st.avatarFrame||'none').toLowerCase()]||'');var fxl=$('[data-pv-fx]',pv);if(fxl){var fxn=String(st.backgroundEffect||'None').toLowerCase();fxl.className='phone__fx'+(fxn!=='none'?' fx-'+fxn:'');}
var pb=$('[data-pv-blocks]',pv);if(pb){var bl=(st.blocks||[]).filter(function(b){return b&&b.enabled!==false&&(b.type==='divider'||(b.type==='clock'&&zoneTime(b.tz))||(b.type==='nowplaying'&&(String(b.text||'').trim()||b.lb))||(b.type==='weather'&&String(b.city||'').trim())||(b.type!=='clock'&&b.type!=='nowplaying'&&b.type!=='weather'&&String(b.text||'').trim()));}).slice(0,6);pb.innerHTML=bl.map(function(b){var t=esc(String(b.text||'').trim().slice(0,90));if(b.type==='clock'){var zt=zoneTime(b.tz);return zt?'<span class="pf__blk pf__blk--now pf__blk--clock">it’s <b>'+esc(zt)+'</b> in '+(t||esc(zoneLabel(b.tz)))+'</span>':'';}if(b.type==='nowplaying'){return'<span class="pf__blk pf__blk--now pf__blk--np"><i class="pf__disc"></i>now playing · '+(t||(b.lb?'live from '+esc(b.lb):''))+'</span>';}if(b.type==='weather'){return'<span class="pf__blk pf__blk--now pf__blk--wx">'+ic('cloud')+'weather in '+(t||esc(String(b.city||'').trim().toLowerCase()))+'</span>';}return b.type==='divider'?'<i class="pf__blk pf__blk--hr">†</i>':b.type==='heading'?'<b class="pf__blk pf__blk--h">'+t+'</b>':b.type==='quote'?'<q class="pf__blk pf__blk--q">'+t+'</q>':b.type==='status'?'<span class="pf__blk pf__blk--now"><i></i>currently · '+t+'</span>':'<span class="pf__blk">'+t+'</span>';}).join('');pb.hidden=!bl.length;}
var fk=String(st.font||'inter').toLowerCase(),fdef=PAGE_FONTS[fk]||PAGE_FONTS.inter;ensureFont(fk,fdef[0]);pv.style.setProperty('--pf-font',fdef[1]);pv.style.setProperty('--pf-fs',String(fdef[2]));var tag=$('[data-pv-tag]',pv);if(tag)tag.hidden=!st.supporterTag;pv.classList.toggle('is-sparkle',!!st.sparkleTrail&&!reduce);pv.classList.toggle('is-gold',!!st.goldBorder);var curUrl=as.cursor&&as.cursor.url,curSet=String(st.cursor||'default').toLowerCase();pv.style.cursor=curUrl?'':(CURSORS[curSet]?'url("'+CURSORS[curSet]+'") 12 12, auto':'');$('[data-pv-views]',pv).textContent=String(p.views||0);var cs=String(st.counterStyle||'plain').toLowerCase(),po=$('[data-pv-odo]',pv),pvw=$('[data-pv-views-wrap]',pv),showV=st.showViews!==false;if(po){var useOdo=showV&&cs!=='plain';po.hidden=!useOdo;po.className='pf__odo pf__odo--'+cs;if(useOdo){var digs=String(p.views||0).replace(/\D/g,'').padStart(6,'0');$('[data-pv-odo-digits]',pv).innerHTML=digs.split('').map(function(d){return'<b>'+d+'</b>';}).join('');}if(pvw)pvw.hidden=!showV||useOdo;}
var ph=$('[data-pv-here]',pv);if(ph)ph.hidden=!st.presence;var ps=$('[data-pv-since]',pv);if(ps){var ca=me&&me.created_at?new Date(me.created_at):null,okd=ca&&!isNaN(ca);ps.hidden=!(st.since&&okd);if(st.since&&okd){var dd=Math.max(0,Math.floor((Date.now()-ca)/864e5)),MN=['jan','feb','mar','apr','may','jun','jul','aug','sep','oct','nov','dec'];ps.textContent='here since '+(dd===0?'today':dd<30?ca.getUTCDate()+' '+MN[ca.getUTCMonth()]:MN[ca.getUTCMonth()]+' '+ca.getUTCFullYear());}}
var pm=$('[data-pv-mood]',pv);if(pm){var hh=new Date().getHours(),mo=hh>=5&&hh<9?'dawn':hh>=9&&hh<17?'day':hh>=17&&hh<21?'dusk':'night';pm.className='phone__mood'+(st.mood?' mood-'+mo:'');var ml=$('[data-cfg="settings.mood"]',dash),mlab=ml&&ml.closest('.toggle');if(mlab)mlab.title='The background wash follows the visitor’s clock: dawn, day, dusk, night — it’s '+mo+' for you right now';}
var sh=$('[data-secret-hint] span:last-child',dash);if(sh){var sw=String((st.secret||{}).word||'').toLowerCase().replace(/[^a-z0-9]/g,''),su=String((st.secret||{}).url||'').trim();sh.textContent=sw.length<3?(sw?'three letters at least':'no secret yet'):!su?'“'+sw+'” — now give it somewhere to go':'“'+sw+'” opens '+su.replace(/^https?:\/\//,'').slice(0,40)+(su.length>48?'…':'');}
var vgs=$('[data-vigil-state]',dash);if(vgs){var vg=String(st.vigil||'').trim();vgs.textContent=!vg||['6','12','24'].indexOf(vg)<0?'off — nothing on your page, nothing kept':'a candle burns for '+vg+' hours, then goes out by itself'+(vigilLit!=null?' · '+(vigilLit===1?'1 is':vigilLit+' are')+' burning right now':'');}
var mnh=$('[data-moon-hint]',dash);if(mnh){if(!st.moon)mnh.textContent='';else{var age=(((Date.now()/86400000+2440587.5)-2451550.1)/29.530588853)%1;if(age<0)age+=1;var lit=(1-Math.cos(2*Math.PI*age))/2;var names=[[0.03,'a new moon'],[0.22,'a waxing crescent'],[0.28,'the first quarter'],[0.47,'a waxing gibbous'],[0.53,'a full moon'],[0.72,'a waning gibbous'],[0.78,'the last quarter'],[0.97,'a waning crescent'],[1.01,'a new moon']];var nm=names.find(function(n){return age<n[0];})[1];mnh.textContent='tonight it’s '+nm+', '+Math.round(lit*100)+'% lit';}}
var nbh=$('[data-nb-hint] span:last-child',dash);if(nbh){var named=((st.neighbours||[]).map(function(n){return String(n||'').trim().toLowerCase();}).filter(Boolean));named=named.filter(function(n,i){return named.indexOf(n)===i;}).slice(0,5);nbh.textContent=!named.length?'nobody named yet — a name only shows if they name you back':named.length+(named.length===1?' name':' names')+' · '+(nbMutual==null?'checking who named you back…':nbMutual+' of them named you back');}
var nih=$('[data-night-hint] span:last-child',dash);if(nih){var nt=String((st.night||{}).tz||'').trim(),nf=+(st.night||{}).from,nu=+(st.night||{}).to;var nb=(st.blocks||[]).filter(function(b){return b&&b.night&&b.enabled!==false;}).length;var okh=nf>=0&&nf<=23&&nu>=0&&nu<=23&&nf!==nu;var zt=nt?zoneTime(nt):null;if(!nt)nih.textContent='no night — nothing is hidden by the hour';else if(!zt)nih.textContent='that doesn’t look like a timezone';else if(!okh)nih.textContent='pick two different hours, 0–23';else{var hr=+String(zt).match(/^(\d+)/)[1]%12,ampm=/pm/.test(zt),h24=(ampm?(hr===0?12:hr+12):(hr===12?0:hr));var dark=nf<nu?(h24>=nf&&h24<nu):(h24>=nf||h24<nu);nih.textContent=(dark?'it’s '+zt+' where you are — your night is on now':'it’s '+zt+' where you are — your night starts at '+nf+':00')
+' · '+(nb?nb+(nb===1?' block is':' blocks are')+' in it':'no blocks marked night only yet');}}
var drh=$('[data-draw-hint] span:last-child',dash);if(drh){var deck=String(st.draw||'').split('\n').map(function(l){return l.trim();}).filter(Boolean).slice(0,12);drh.textContent=!deck.length?'no deck — nothing is dealt':deck.length===1?'one line, so everybody gets that one':deck.length+' lines · a visitor gets one of them, and keeps it for the day';}
var tlh=$('[data-tally-hint] span:last-child',dash);if(tlh){var tq=String((st.tally||{}).q||'').trim();var tos=((st.tally||{}).options||[]).map(function(o){return String(o||'').trim();}).filter(Boolean);var uniq=tos.filter(function(o,i){return tos.map(function(x){return x.toLowerCase();}).indexOf(o.toLowerCase())===i;});tlh.textContent=!tq?'not asking anything — the tally stays off':uniq.length<2?'give it at least two answers':'asking · '+uniq.length+' answers'+(uniq.length<tos.length?' (two the same, one dropped)':'');}
var bkh=$('[data-back-hint] span:last-child',dash);if(bkh){var rv=st.reverse||{},rt=String(rv.text||'').trim(),rn=String(rv.title||'').trim();var bb=(st.blocks||[]).filter(function(b){return b&&b.enabled!==false&&String(b.place||'')==='back';}).length;bkh.textContent=!rt&&!rn&&!bb?'no back yet — the card stays one-sided':(rt||rn?'the corner turns over to “'+(rn||'the other side')+'”':'the corner turns over')+(bb?' · '+bb+(bb===1?' block is':' blocks are')+' round there':'');}
var skh=$('[data-sky-hint] span:last-child',dash);if(skh){var sc=String(st.sky||'').trim();var wxb=((st.blocks||[]).filter(function(b){return b&&String(b.type||'').toLowerCase()==='weather'&&String(b.city||'').trim();})[0]||{}).city;skh.textContent=!sc?(wxb?'no sky — your weather block says '+wxb:'no sky'):'the sky over '+sc+', checked every ten minutes';}
var ch=$('[data-capsule-hint] span:last-child',dash);if(ch){var ct=String((st.capsule||{}).text||'').trim(),ca=String((st.capsule||{}).at||'').trim(),cd=ca?Date.parse(ca+'T00:00:00Z'):NaN;ch.textContent=!ct?'nothing sealed yet':!ca?'pick the day it opens':isNaN(cd)?'that date doesn’t look right':(cd<=Date.now()?'open — your page is showing it':(function(){var d=Math.ceil((cd-Date.now())/86400000);return'sealed · opens in '+d+(d===1?' day':' days');})());}
var live=(cfg.socials||[]).filter(function(s){return s.enabled!==false&&String(s.value||'').trim();}),links=live.slice(0,4);$('[data-pv-links]',pv).innerHTML=(st.showSocials===false?[]:links).map(function(s){return'<span class="pf__link">'+ic(PLAT_ICON[s.platform]||'link')+'<span class="pf__label">'+esc(s.label||s.platform)+'</span>'+ic('arrow')+'</span>';}).join('')
+(st.showSocials!==false&&live.length>4?'<span class="pf__more">+'+(live.length-4)+' more on the page</span>':'');pv.querySelector('.pf__name').className='pf__name'+({Gradient:' fx-gradient',Shimmer:' fx-shimmer',Glow:' fx-glow'}[st.usernameEffect]||'')+(st.usernameGlow?' fx-halo':'');var hasBadge=(cfg.badges||[]).some(function(b){return b.enabled!==false&&b.owned!==false;}),bEl=$('[data-pv-badge]',pv);if(bEl){bEl.hidden=!hasBadge;bEl.className='pf__badge'+(rank(me&&me.plan)>=2?' pf__badge--gold':'');}}
var liveViews=null;function stats(){var links=(cfg.socials||[]).filter(function(s){return s.enabled!==false&&String(s.value||'').trim();}).length,badges=(cfg.badges||[]).filter(function(b){return b.enabled&&b.owned;}).length;$('[data-stat-views]').textContent=liveViews!=null?fmt(liveViews):String((cfg.profile&&cfg.profile.views)||0);$('[data-stat-links]').textContent=String(links);$('[data-stat-badges]').textContent=String(badges);}
function status(text,note,ok){$('[data-stat-status]').textContent=text;$('[data-stat-status-note]').textContent=note;$('[data-stat-status]').style.color=ok?'var(--green)':'';published=!!ok;}
var savedTimer=null,loadFailed=false;function refreshStatus(justSaved){clearTimeout(savedTimer);if(loadFailed){status('—','couldn’t load — refresh to try again',false);return;}
if(!me||!me.username){status('no username','pick one under Account to publish',false);return;}
if(!published){status('not published','fill in your page and hit save',false);return;}
if(justSaved){status('live','saved just now',true);savedTimer=setTimeout(function(){if(!dirty)status('live','your page is published',true);},8000);return;}
status('live',dirty?'unsaved changes — hit save':'your page is published',true);var vn=$('[data-stat-views-note]');if(vn&&/went live|once your page/.test(vn.textContent))vn.textContent='since your page went live';}
var published=false,clEl=$('[data-checklist]',dash);function checklist(settle){if(!clEl)return;var p=cfg.profile||{};var done={name:!!(me&&me.username),about:!!(p.displayName&&p.description),link:(cfg.socials||[]).some(function(s){return s.enabled!==false&&!!String(s.value||'').trim();})};done.save=published&&!dirty;Object.keys(done).forEach(function(k){var li=$('[data-check="'+k+'"]',clEl);if(li){li.classList.toggle('is-done',done[k]);var sr=$('[data-check-state]',li);if(sr)sr.textContent=done[k]?'done: ':'';}});if(settle)clEl.hidden=done.name&&done.about&&done.link&&done.save;}
var RANK={free:0,lifetime:1,supporter:2};function rank(p){p=(p||'free').toLowerCase();return p in RANK?RANK[p]:1;}
function applyPlan(){var plan=(me&&me.plan||'free').toLowerCase(),r=rank(plan),name=$('[data-plan-name]'),note=$('[data-plan-note]'),copy=$('[data-plan-copy]'),cta=$('[data-plan-cta]');if(name)name.textContent=plan==='supporter'?'Supporter':plan==='lifetime'?'Lifetime':plan==='free'?'Free':plan.charAt(0).toUpperCase()+plan.slice(1);if(note)note.textContent=plan==='free'?'forever':(me&&me.plan_expires_at?'until '+new Date(me.plan_expires_at).toLocaleDateString():'yours for life');if(copy)copy.textContent=r>=2?'Everything unlocked. Thank you for keeping the lights on.':r===1?'Supporter ($9.99, one-time) adds the gold check, halo and cursors.':'Lifetime ($5) and Supporter ($9.99) are one-time upgrades — nothing renews.';if(cta){cta.hidden=r>=2;cta.href=r===1?'/pricing#supporter':'/pricing#lifetime';cta.textContent=r===1?'Get Supporter':'See upgrades';}
$$('option[data-plan]',dash).forEach(function(o){o.disabled=r<rank(o.dataset.plan);});$$('[data-plan]',dash).forEach(function(el){if(el.tagName==='OPTION')return;var need=rank(el.dataset.plan),locked=r<need;el.disabled=locked;if(!('descBase'in el.dataset))el.dataset.descBase=el.getAttribute('aria-describedby')||'';var descs=el.dataset.descBase?[el.dataset.descBase]:[];if(locked)descs.push('lock-'+el.dataset.plan);if(descs.length)el.setAttribute('aria-describedby',descs.join(' '));else el.removeAttribute('aria-describedby');var wrap=el.closest('.field')||el.closest('.toggle');if(!wrap)return;wrap.classList.toggle('is-locked',locked);var lab=$('.field__label span',wrap);if(lab&&!lab.dataset.base)lab.dataset.base=lab.textContent;var fl=$('label',wrap),fname=fl?fl.textContent.trim():'';if(lab)lab.innerHTML=locked?'<a href="'+((DEMO&&DEMO.pricing)||'/pricing')+'#'+el.dataset.plan+'"'+(fname?' aria-label="'+esc(fname)+' — '+esc(lab.dataset.base)+', unlock"':'')+'>'+esc(lab.dataset.base)+' — unlock</a>':esc(lab.dataset.base);});}
var PRESETS={gothic:{accentColor:'#F00646',textColor:'#F2F2F0',backgroundColor:'#050606',iconColor:'#F00646',profileRadius:28,profileGradient:true,backgroundEffect:'Glow',usernameEffect:'Shimmer',profileOpacity:.72,profileBlur:16,backgroundOpacity:.55},soft:{accentColor:'#ff6ea9',textColor:'#3b2a33',backgroundColor:'#fff1f6',iconColor:'#ff6ea9',profileRadius:22,profileGradient:true,backgroundEffect:'Glow',usernameEffect:'Gradient',profileOpacity:.8,profileBlur:14,backgroundOpacity:.5},mono:{accentColor:'#ffffff',textColor:'#ffffff',backgroundColor:'#000000',iconColor:'#ffffff',profileRadius:2,profileGradient:false,backgroundEffect:'None',usernameEffect:'None',profileOpacity:.35,profileBlur:0,backgroundOpacity:.6},paper:{accentColor:'#141210',textColor:'#141210',backgroundColor:'#EEE8E2',iconColor:'#141210',profileRadius:4,profileGradient:false,backgroundEffect:'None',usernameEffect:'None',profileOpacity:.8,profileBlur:0,backgroundOpacity:.75},sunset:{accentColor:'#ff8a3d',textColor:'#fff3e6',backgroundColor:'#1a0b2e',iconColor:'#ff8a3d',profileRadius:20,profileGradient:true,backgroundEffect:'Stars',usernameEffect:'Gradient',profileOpacity:.55,profileBlur:18,backgroundOpacity:.85},y2k:{accentColor:'#39ffa0',textColor:'#c8ffe6',backgroundColor:'#03060f',iconColor:'#39ffa0',profileRadius:6,profileGradient:true,backgroundEffect:'Particles',usernameEffect:'Glow',profileOpacity:.5,profileBlur:10,backgroundOpacity:.7},vampire:{accentColor:'#b3001b',textColor:'#f3e9e9',backgroundColor:'#0b0004',iconColor:'#b3001b',profileRadius:18,profileGradient:true,backgroundEffect:'Embers',usernameEffect:'Glow',profileOpacity:.6,profileBlur:14,backgroundOpacity:.7,linkStyle:'outline',avatarFrame:'ring',font:'unifraktur',playerStyle:'vinyl'},cathedral:{accentColor:'#d4a24c',textColor:'#efe6d2',backgroundColor:'#0e0c14',iconColor:'#d4a24c',profileRadius:8,profileGradient:true,backgroundEffect:'Stars',usernameEffect:'Gradient',profileOpacity:.62,profileBlur:12,backgroundOpacity:.6,linkStyle:'ghost',avatarFrame:'halo',font:'cormorant',playerStyle:'minimal'},static:{accentColor:'#e8e8e8',textColor:'#f5f5f5',backgroundColor:'#101010',iconColor:'#e8e8e8',profileRadius:0,profileGradient:false,backgroundEffect:'Rain',usernameEffect:'Glow',profileOpacity:.4,profileBlur:4,backgroundOpacity:.8,linkStyle:'brutal',avatarFrame:'pixel',font:'vt323',playerStyle:'bars'},lavender:{accentColor:'#7c5cff',textColor:'#2a2140',backgroundColor:'#f3eefc',iconColor:'#7c5cff',profileRadius:30,profileGradient:true,backgroundEffect:'Petals',usernameEffect:'Gradient',profileOpacity:.8,profileBlur:16,backgroundOpacity:.5,linkStyle:'outline',avatarFrame:'petals',font:'playfair',playerStyle:'minimal'}};var LOOK_DEFAULTS={linkStyle:'pill',avatarFrame:'none',font:'inter',playerStyle:'bars'};for(var pk in PRESETS){for(var lk in LOOK_DEFAULTS){if(!(lk in PRESETS[pk]))PRESETS[pk][lk]=LOOK_DEFAULTS[lk];}}
function applyPreset(name,quiet){var pr=PRESETS[name];if(!pr)return;cfg.settings=Object.assign({},cfg.settings,pr);lastTheme=name;hydrate();syncTheme();if(!quiet){touch();msg('Theme “'+name+'” applied — hit save when you like it.','ok');}}
var lastTheme=null;function themeOf(st){st=st||{};for(var n in PRESETS){var pr=PRESETS[n],ok=true;for(var k in pr){if(String(st[k]).toLowerCase()!==String(pr[k]).toLowerCase()){ok=false;break;}}if(ok)return n;}return null;}
function syncTheme(){var cur=themeOf(cfg.settings);if(cur)lastTheme=cur;$$('[data-theme-pick]',dash).forEach(function(b){b.setAttribute('aria-pressed',String(b.dataset.themePick===cur));});var rb=$('[data-theme-reset]',dash);if(rb){rb.hidden=!(lastTheme&&!cur);var nm=$('[data-theme-reset-name]',rb);if(nm)nm.textContent=lastTheme||'the theme';}}
var resetBtn=$('[data-theme-reset]',dash);if(resetBtn)resetBtn.addEventListener('click',function(){if(lastTheme)applyPreset(lastTheme);});$$('[data-theme-pick]',dash).forEach(function(b){b.addEventListener('click',function(){applyPreset(b.dataset.themePick);});});function themeFromSignup(){var t=null;try{t=sessionStorage.getItem('misaTheme');sessionStorage.removeItem('misaTheme');}catch(e){}var q=new URLSearchParams(location.search).get('theme');if(PRESETS[q])t=q;return t;}
function cardKey(){var str=JSON.stringify(editable(cfg)),h=5381;for(var i=0;i<str.length;i++)h=((h<<5)+h+str.charCodeAt(i))|0;return(h>>>0).toString(16);}
function applyShare(){var sh=$('[data-share]',dash);if(!sh)return;var u=me&&me.username;sh.hidden=!u;if(!u)return;var img=$('img',sh),next=DEMO?(DEMO.card||''):'/'+encodeURIComponent(u)+'/card.png?v='+cardKey();if(img.getAttribute('src')!==next)img.src=next;img.alt='Share card for misa.lol/'+u;}
var ana=$('[data-analytics]',dash);function fmt(n){n=+n||0;return n>=1e6?(n/1e6).toFixed(1)+'m':n>=1e4?(n/1e3).toFixed(1)+'k':String(n);}
function renderStats(st){if(!st)return;cfg.profile.views=st.views||0;liveViews=st.views||0;var msBox=$('[data-milestone]',dash);if(msBox){var MS=[100,1000,10000,100000,1000000],hitMs=MS.filter(function(m){return(+st.views||0)>=m;}).pop(),nextMs=MS.filter(function(m){return(+st.views||0)<m;})[0];msBox.hidden=!hitMs||!!st.unavailable;if(hitMs){var LINE={100:'a hundred people found this.',1000:'a thousand. it’s a place now.',10000:'ten thousand. you’re on the map.',100000:'a hundred thousand. I just need your name.',1000000:'a million. what.'};$('[data-ms-num]',msBox).textContent=hitMs.toLocaleString()+' views.';$('[data-ms-note]',msBox).textContent=LINE[hitMs]+(nextMs?' next: '+nextMs.toLocaleString()+'.':'');var mu=location.origin+'/'+encodeURIComponent(me&&me.username||'')+'/milestone.png?n='+hitMs,mo=$('[data-ms-open]',msBox);if(mo){mo.href=mu;mo.hidden=!!DEMO;}msBox.dataset.url=mu;}}
$('[data-stat-views]').textContent=fmt(st.views);var note=$('[data-stat-views-note]');if(note)note.textContent=st.unavailable?'analytics unavailable right now':(fmt(st.today)+' today · '+fmt(st.week)+' this week');if(!ana)return;ana.hidden=!!st.unavailable;if(st.unavailable)return;var daily=st.daily||[],max=Math.max.apply(null,daily.map(function(d){return d[1];}).concat([1])),sum14=daily.reduce(function(a,d){return a+d[1];},0);function nice(iso){var day=new Date(iso+'T00:00:00Z');return day.toLocaleDateString(undefined,{weekday:'short',day:'numeric',month:'short',timeZone:'UTC'});}
var bars=$('[data-ana-bars]',ana),cap=$('[data-ana-cap]',ana);bars.innerHTML=daily.length&&sum14>0?daily.map(function(d){var day=new Date(d[0]+'T00:00:00Z');return'<button type="button" class="ana__bar" style="--h:'+Math.round(d[1]/max*100)+'%" data-day="'+esc(d[0])+'" data-n="'+d[1]+'" aria-label="'+esc(nice(d[0]))+': '+d[1]+(d[1]===1?' view':' views')+'"><i></i><b>'+['S','M','T','W','T','F','S'][day.getUTCDay()]+'</b></button>';}).join(''):'<p class="ana__empty">'+((+st.views||0)>0?'no views in the last 14 days':'no views yet — the bars fill in as people visit')+'</p>';var capBase='last 14 days · '+fmt(sum14)+' views';if(cap)cap.textContent=capBase;$$('.ana__bar',bars).forEach(function(b){b.addEventListener('click',function(){var on=b.classList.contains('is-on');$$('.ana__bar',bars).forEach(function(x){x.classList.remove('is-on');});if(cap)cap.textContent=on?capBase:nice(b.dataset.day)+' · '+b.dataset.n+' views';if(!on)b.classList.add('is-on');});});var refs=st.referrers||[],clicks=st.clicks||[];var attributed=refs.reduce(function(a,r){return a+(+r[1]||0);},0),direct=Math.max(0,(+st.views||0)-attributed);var unit=function(n,w){return'<span class="sr-only"> '+(+n===1?w:w+'s')+'</span>';};var refRows=refs.slice(0,6).map(function(r){return'<li><span>'+esc(r[0])+'</span> <b>'+fmt(r[1])+unit(r[1],'view')+'</b></li>';});if(direct>0)refRows.unshift('<li><span>direct or from apps</span> <b>'+fmt(direct)+unit(direct,'view')+'</b></li>');$('[data-ana-refs]',ana).innerHTML=refRows.length?refRows.join(''):'<li class="ana__empty">no visits yet</li>';$('[data-ana-clicks]',ana).innerHTML=clicks.length?clicks.slice(0,6).map(function(c){return'<li><span>'+esc(c[1]||c[0])+'</span> <b>'+fmt(c[2])+unit(c[2],'click')+'</b></li>';}).join(''):'<li class="ana__empty">no clicks yet</li>';}
function loadStats(){api('/api/v1/me/stats',null).then(renderStats).catch(function(){if(ana)ana.hidden=true;});}
var repEl=$('[data-replay]',dash),repMsg=$('[data-replay-msg]',dash);var REP_WORD={in:'arrived',tap:'tapped',sign:'signed the guestbook',draw:'chalked the board'};function ago(sec){sec=Math.max(0,Math.round(sec));if(sec<60)return sec+'s ago';var m=Math.round(sec/60);if(m<60)return m+(m===1?' minute ago':' minutes ago');var h=Math.round(m/60);if(h<24)return h+(h===1?' hour ago':' hours ago');var d=Math.round(h/24);return d+(d===1?' day ago':' days ago');}
function after(sec){return sec<1?'straight away':(sec<60?sec+'s in':Math.round(sec/60)+' min in');}
function linkLabel(id){var hit=(cfg.socials||[]).filter(function(x){return String(x.id)===String(id);})[0];return hit?(hit.label||hit.platform||'a link'):null;}
function renderReplay(d){if(!repEl)return;if(!d||d.unavailable){repEl.hidden=true;repMsg.hidden=false;repMsg.textContent='The replay can’t be read right now — the counts above are unaffected.';return;}
if(!d.on){repEl.hidden=true;repMsg.hidden=false;repMsg.textContent='Off. Flick “keep a replay” and save — the next visit starts the list.';return;}
var vs=d.visits||[];if(!vs.length){repEl.hidden=true;repMsg.hidden=false;repMsg.textContent='On, and waiting for someone. Visits show up here for '+(d.kept_for_days||7)+' days.';return;}
repMsg.hidden=true;repEl.hidden=false;var now=Date.now()/1000;repEl.innerHTML=vs.map(function(v){var head='<span class="replay__when">'+esc(ago(now-v.at))+'</span>'+'<span class="replay__from">'+(v.from?'from '+esc(v.from):'straight to your link')+'</span>'+'<span class="replay__dev">'+esc(v.device)+'</span>'+
(v.seconds>=5?'<span class="replay__stay">stayed '+esc(v.seconds<60?v.seconds+'s':Math.round(v.seconds/60)+' min')+'</span>':'');var steps=(v.events||[]).map(function(e){var what=REP_WORD[e.k]||e.k;if(e.k==='tap'){var lab=linkLabel(e.d);what=lab?('tapped <b>'+esc(lab)+'</b>'):'tapped a link that’s gone now';}
return'<li class="replay__step replay__step--'+esc(e.k)+'">'+what+(e.after>2?' <span class="replay__at">'+esc(after(e.after))+'</span>':'')+'</li>';}).join('');return'<li class="replay__visit"><div class="replay__head">'+head+'</div><ol class="replay__steps">'+steps+'</ol></li>';}).join('');}
function loadReplay(){if(!repEl||!me||!me.username)return;api('/api/v1/me/replay',null).then(renderReplay).catch(function(){renderReplay(null);});}
var ACCEPT={avatar:'image/png,image/jpeg,image/webp,image/gif',background:'image/png,image/jpeg,image/webp,image/gif',video:'video/mp4,video/webm',audio:'audio/mpeg,audio/ogg,audio/wav,audio/mp4,.mp3,.ogg,.wav,.m4a',cursor:'image/png'};var UPLOAD_PLAN={cursor:2};var UPLOAD_MAX={avatar:8,background:8,video:32,audio:12,cursor:0.25};var UPLOAD_TYPES={avatar:'png, jpg, webp or gif',background:'png, jpg, webp or gif',video:'mp4 or webm',audio:'mp3, ogg, wav or m4a',cursor:'png'};var UPLOAD_WHAT={avatar:'avatar images',background:'background images',video:'background videos',audio:'audio files',cursor:'cursor images'};function fmtMB(n){return n>=1?(Math.round(n*10)/10)+' MB':Math.round(n*1024)+' KB';}
function typeOk(kind,f){var acc=(ACCEPT[kind]||'').split(',').filter(Boolean);if(!acc.length)return true;var ext='.'+String(f.name||'').split('.').pop().toLowerCase(),mime=String(f.type||'').toLowerCase();return acc.some(function(a){return a.charAt(0)==='.'?a===ext:(mime===a||(!mime&&acc.every(function(x){return x.charAt(0)!=='.';})));});}
function uploadNote(b,html,kind){var f=b.closest('.field'),note=f&&$('[data-asset-note]',f);if(!note)return;note.hidden=false;$('span:last-child',note).innerHTML=html;f.classList.toggle('is-invalid',kind==='error');f.classList.toggle('is-valid',kind==='ok');}
function applyUploads(){var r=rank(me&&me.plan);$$('[data-upload]',dash).forEach(function(b){var need=UPLOAD_PLAN[b.dataset.upload]||1,locked=r<need;b.disabled=false;if(locked){b.setAttribute('aria-disabled','true');b.setAttribute('aria-describedby',need>=2?'lock-supporter':'lock-uploads');}else{b.removeAttribute('aria-disabled');b.removeAttribute('aria-describedby');}
b.title=locked?('Uploads come with '+(need>=2?'Supporter':'Lifetime')+' — a link works on every plan'):b.getAttribute('aria-label');});}
$$('[data-upload]',dash).forEach(function(b){var inp=null,kind=b.dataset.upload;function fail(t){uploadNote(b,esc(t),'error');sayDash(t);}
function send(f){var target=document.getElementById(b.dataset.target);if(!target)return;var max=UPLOAD_MAX[kind]||0;if(max&&f.size>max*1048576){fail('that’s '+fmtMB(f.size/1048576)+' — '+UPLOAD_WHAT[kind]+' can be up to '+fmtMB(max));return;}
if(!typeOk(kind,f)){fail('that isn’t a '+UPLOAD_TYPES[kind]+' file');return;}
b.setAttribute('aria-busy','true');b.classList.add('is-busy');var done=function(){b.removeAttribute('aria-busy');b.classList.remove('is-busy');};var landed=function(url,note,say){target.value=url;target.dispatchEvent(new Event('input',{bubbles:true}));uploadNote(b,note,'ok');sayDash(say);done();};uploadNote(b,'uploading '+esc(f.name)+'…','busy');sayDash('Uploading '+f.name+'.');if(DEMO){setTimeout(function(){landed(URL.createObjectURL(f),'demo: '+esc(f.name)+' is previewed locally, not uploaded','Demo: '+f.name+' is previewed locally, not uploaded.');},400);return;}
var fd=new FormData();fd.append('file',f);fd.append('kind',kind);var x=new XMLHttpRequest();x.open('POST','/api/v1/uploads');x.withCredentials=true;x.responseType='text';if(x.upload)x.upload.addEventListener('progress',function(e){if(e.lengthComputable&&e.total>0){var pc=Math.round(e.loaded/e.total*100);if(pc<100)uploadNote(b,'uploading '+esc(f.name)+' — '+pc+'%','busy');}});x.addEventListener('load',function(){var d={};try{d=JSON.parse(x.responseText||'{}');}catch(e){}if(x.status<200||x.status>=300||!d||!d.url){fail((d&&typeof d.detail==='string')?d.detail:'Upload failed.');done();return;}landed(d.url,'uploaded '+esc(f.name)+' — hit save to publish it','Uploaded '+f.name+' — hit save to publish it.');});var lost=function(){fail('could not reach the server — check the connection and try again');done();};x.addEventListener('error',lost);x.addEventListener('abort',lost);x.addEventListener('timeout',lost);x.send(fd);}
b.addEventListener('click',function(){if(b.getAttribute('aria-busy')==='true')return;if(b.getAttribute('aria-disabled')==='true'){var need=UPLOAD_PLAN[kind]||1,plan=need>=2?'supporter':'lifetime',label=need>=2?'Supporter':'Lifetime';uploadNote(b,'uploads come with <a href="'+((DEMO&&DEMO.pricing)||'/pricing')+'#'+plan+'">'+label+'</a> — a link works on every plan','info');sayDash('Uploads come with '+label+' — a link works on every plan.');return;}
if(!inp){inp=document.createElement('input');inp.type='file';inp.accept=ACCEPT[kind]||'';inp.hidden=true;inp.tabIndex=-1;inp.setAttribute('aria-hidden','true');document.body.appendChild(inp);inp.addEventListener('change',function(){var f=inp.files&&inp.files[0];if(f)send(f);});}
inp.value='';inp.click();});});var bookPending=$('[data-book-pending]',dash),bookOk=$('[data-book-ok]',dash),bookLive=$('[data-book-live]',dash),bookMsg=$('[data-book-msg]',dash);function sayBook(t){if(!bookLive)return;bookLive.textContent='';setTimeout(function(){bookLive.textContent=t;},30);}
function bookWhen(ts){try{return new Date(ts*1000).toLocaleDateString(undefined,{day:'numeric',month:'short'});}catch(e){return'';}}
function slip(e,pendingRow){return'<div class="slip-row" data-slip="'+esc(String(e.id))+'"><div class="slip-row__text"><span class="slip-row__line">'+esc(String(e.line||''))+'</span><span class="slip-row__who">— '+esc(String(e.name||''))+' · '+bookWhen(e.at)+'</span></div><span class="slip-row__acts">'+(pendingRow?'<button class="btn btn--ivory btn--sm" type="button" data-book-approve aria-label="Approve the line from '+esc(String(e.name||''))+'">Approve</button>':'')+'<button class="btn btn--ghost btn--sm" type="button" data-book-bin aria-label="Bin the line from '+esc(String(e.name||''))+'">Bin</button></span></div>';}
function renderBook(data){if(!bookPending||!bookOk)return;var pend=data.pending||[],oks=data.approved||[];bookPending.innerHTML=pend.length?pend.map(function(e){return slip(e,true);}).join(''):'<p class="dash__previewnote" style="text-align:left">nothing waiting.</p>';bookOk.innerHTML=oks.length?oks.map(function(e){return slip(e,false);}).join(''):'<p class="dash__previewnote" style="text-align:left">no signatures yet.</p>';var pc=$('[data-book-pending-count]',dash),oc=$('[data-book-ok-count]',dash);if(pc)pc.textContent=pend.length?pend.length+' new':'';if(oc)oc.textContent=oks.length?oks.length+(oks.length===1?' signature':' signatures'):'';$$('[data-book-approve]',bookPending).forEach(function(b){b.addEventListener('click',function(){bookAct(b,'approve');});});$$('[data-book-bin]',dash).forEach(function(b){b.addEventListener('click',function(){bookAct(b,'bin');});});}
var bookData={pending:[],approved:[]};function bookAct(btn,what){var row=btn.closest('[data-slip]'),id=row&&row.dataset.slip;if(!id||btn.getAttribute('aria-busy')==='true')return;btn.setAttribute('aria-busy','true');var bookSec=$('#visitors',dash);(DEMO?Promise.resolve({ok:true}):fetch('/api/v1/me/guestbook/'+encodeURIComponent(id)+(what==='approve'?'/approve':''),{method:what==='approve'?'POST':'DELETE',credentials:'include',headers:{Accept:'application/json'}})).then(function(res){if(!res.ok){formMsg(bookSec,'Could not do that — try again.','error');return;}
var from=bookData.pending.filter(function(e){return e.id===id;})[0]||bookData.approved.filter(function(e){return e.id===id;})[0];bookData.pending=bookData.pending.filter(function(e){return e.id!==id;});bookData.approved=bookData.approved.filter(function(e){return e.id!==id;});if(what==='approve'&&from)bookData.approved.unshift(from);var nxt=row.nextElementSibling||row.previousElementSibling;renderBook(bookData);sayBook(what==='approve'?'Approved — it’s on your page now.':'Binned.');var f=nxt&&$('[data-slip="'+nxt.dataset.slip+'"] button',dash);(f||$('[data-cfg="settings.guestbook"]',dash)).focus();}).catch(function(){formMsg(bookSec,'Could not reach the server.','error');}).then(function(){btn.removeAttribute('aria-busy');});}
function loadBook(){if(!bookPending||!me||!me.username)return;api('/api/v1/me/guestbook',null).then(function(d){if(d){bookData={pending:d.pending||[],approved:d.approved||[]};renderBook(bookData);}}).catch(function(){});}
var boardPending=$('[data-board-pending]',dash),boardOk=$('[data-board-ok]',dash),boardLive=$('[data-board-live]',dash),boardData={pending:[],approved:[]};function sayBoard(t){if(!boardLive)return;boardLive.textContent='';setTimeout(function(){boardLive.textContent=t;},30);}
function safeSvg(x){x=String(x||'');return/^<svg[\s\S]*<\/svg>$/.test(x)&&!/<script|on[a-z]+=|<foreignObject|<image|href=/i.test(x)?x:'';}
function tile(e,pendingRow){return'<div class="board-tile" data-tile="'+esc(String(e.id))+'"><div class="board-tile__pic">'+safeSvg(e.svg)+'</div><div class="board-tile__acts"><span class="board-tile__when">'+bookWhen(e.at)+'</span>'+(pendingRow?'<button class="btn btn--ivory btn--sm" type="button" data-board-approve aria-label="Keep this drawing">Keep</button>':'')+'<button class="btn btn--ghost btn--sm" type="button" data-board-bin aria-label="Bin this drawing">Bin</button></div></div>';}
function renderBoard(data){if(!boardPending||!boardOk)return;var pend=data.pending||[],oks=data.approved||[];boardPending.innerHTML=pend.length?pend.map(function(e){return tile(e,true);}).join(''):'<p class="dash__previewnote" style="text-align:left">nothing waiting.</p>';boardOk.innerHTML=oks.length?oks.map(function(e){return tile(e,false);}).join(''):'<p class="dash__previewnote" style="text-align:left">nothing on the wall yet.</p>';var pc=$('[data-board-pending-count]',dash),oc=$('[data-board-ok-count]',dash);if(pc)pc.textContent=pend.length?pend.length+' new':'';if(oc)oc.textContent=oks.length?oks.length+(oks.length===1?' drawing':' drawings'):'';$$('[data-board-approve]',boardPending).forEach(function(b){b.addEventListener('click',function(){boardAct(b,'approve');});});$$('[data-board-bin]',dash).forEach(function(b){b.addEventListener('click',function(){boardAct(b,'bin');});});}
function boardAct(btn,what){var row=btn.closest('[data-tile]'),id=row&&row.dataset.tile;if(!id||btn.getAttribute('aria-busy')==='true')return;btn.setAttribute('aria-busy','true');(DEMO?Promise.resolve({ok:true}):fetch('/api/v1/me/doodles/'+encodeURIComponent(id)+(what==='approve'?'/approve':''),{method:what==='approve'?'POST':'DELETE',credentials:'include',headers:{Accept:'application/json'}})).then(function(res){if(!res.ok){partMsg('[data-board-msg]','Could not do that — try again.','error');return;}
var from=boardData.pending.filter(function(e){return e.id===id;})[0]||boardData.approved.filter(function(e){return e.id===id;})[0];boardData.pending=boardData.pending.filter(function(e){return e.id!==id;});boardData.approved=boardData.approved.filter(function(e){return e.id!==id;});if(what==='approve'&&from)boardData.approved.unshift(from);var nxt=row.nextElementSibling||row.previousElementSibling;renderBoard(boardData);sayBoard(what==='approve'?'Kept — it’s on the wall now.':'Binned.');var f=nxt&&nxt.dataset&&$('[data-tile="'+nxt.dataset.tile+'"] button',dash);(f||$('[data-cfg="settings.doodles"]',dash)).focus();}).catch(function(){partMsg('[data-board-msg]','Could not reach the server.','error');}).then(function(){btn.removeAttribute('aria-busy');});}
function loadBoard(){if(!boardPending||!me||!me.username)return;api('/api/v1/me/doodles',null).then(function(d){if(d){boardData={pending:d.pending||[],approved:d.approved||[]};renderBoard(boardData);}}).catch(function(){});}
var askNew=$('[data-ask-new]',dash),askDone=$('[data-ask-done]',dash),askLive=$('[data-ask-live]',dash),askData={waiting:[],answered:[]};function partMsg(sel,t,kind){var box=$(sel,dash);if(!box){msg(t,kind);return;}box.hidden=false;box.textContent=t;box.classList.add('is-visible');box.classList.toggle('auth-message--error',kind==='error');box.classList.toggle('auth-message--ok',kind!=='error');}
function askMsg(t,kind){partMsg('[data-ask-msg]',t,kind);}
function sayAsk(t){if(!askLive)return;askLive.textContent='';setTimeout(function(){askLive.textContent=t;},30);}
function askWhen(at){var s=Math.max(0,Math.round(Date.now()/1000-(+at||0)));if(s<3600)return Math.max(1,Math.round(s/60))+'m ago';if(s<86400)return Math.round(s/3600)+'h ago';return Math.round(s/86400)+'d ago';}
function askRow(e,answeredAlready){var a=esc(e.a||'');return'<div class="ask-card" data-ask="'+esc(e.id)+'">'+'<p class="ask-card__q"><span>'+esc(e.q||'')+'</span><span class="ask-card__when">'+esc(askWhen(e.at))+'</span></p>'+'<div class="ask-card__row">'+'<label class="field ask-card__field"><span class="sr-only">Your answer to “'+esc(e.q||'')+'”</span>'+'<span class="field__box"><input class="field__input" type="text" maxlength="400" value="'+a+'" placeholder="'+(answeredAlready?'change your answer':'answer it — this is what goes on your page')+'" data-ask-input></span></label>'+'<button class="btn btn--crimson btn--sm" type="button" data-ask-send>'+(answeredAlready?'Save':'Answer')+'</button>'+'<button class="btn btn--ghost btn--sm" type="button" data-ask-bin>Bin</button>'+'</div></div>';}
function renderAsks(data){if(!askNew||!askDone)return;var w=data.waiting||[],d=data.answered||[];askNew.innerHTML=w.length?w.map(function(e){return askRow(e,false);}).join(''):'<p class="dash__previewnote" style="text-align:left">nobody has asked anything yet.</p>';askDone.innerHTML=d.length?d.map(function(e){return askRow(e,true);}).join(''):'<p class="dash__previewnote" style="text-align:left">nothing answered yet.</p>';var nc=$('[data-ask-new-count]',dash),dc=$('[data-ask-done-count]',dash);if(nc)nc.textContent=w.length?w.length+' waiting':'';if(dc)dc.textContent=d.length?d.length+(d.length===1?' answer':' answers'):'';$$('[data-ask-send]',dash).forEach(function(b){b.addEventListener('click',function(){askSend(b);});});$$('[data-ask-bin]',dash).forEach(function(b){b.addEventListener('click',function(){askBin(b);});});$$('[data-ask-input]',dash).forEach(function(i){i.addEventListener('keydown',function(e){if(e.key==='Enter'){e.preventDefault();askSend($('[data-ask-send]',i.closest('[data-ask]')));}});});}
function askSend(btn){if(!btn||btn.getAttribute('aria-busy')==='true')return;var card=btn.closest('[data-ask]'),id=card&&card.dataset.ask,input=card&&$('[data-ask-input]',card),text=input?input.value.trim():'';if(!id)return;if(!text){askMsg('Write an answer first — that’s what goes on your page.','error');input&&input.focus();return;}
btn.setAttribute('aria-busy','true');(DEMO?Promise.resolve({ok:true}):fetch('/api/v1/me/asks/'+encodeURIComponent(id)+'/answer',{method:'POST',credentials:'include',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify({answer:text})})).then(function(res){if(!res.ok){askMsg('Could not save that answer — try again.','error');return;}
var was=askData.waiting.filter(function(e){return e.id===id;})[0];if(was){askData.waiting=askData.waiting.filter(function(e){return e.id!==id;});was.a=text;askData.answered.unshift(was);sayAsk('Answered — it’s on your page now.');}
else{askData.answered.forEach(function(e){if(e.id===id)e.a=text;});sayAsk('Answer updated.');}
renderAsks(askData);var f=$('[data-ask-input]',askNew)||$('[data-cfg="settings.asks"]',dash);if(f)f.focus();}).catch(function(){askMsg('Could not reach the server.','error');}).then(function(){btn.removeAttribute('aria-busy');});}
function askBin(btn){if(!btn||btn.getAttribute('aria-busy')==='true')return;var card=btn.closest('[data-ask]'),id=card&&card.dataset.ask;if(!id)return;btn.setAttribute('aria-busy','true');(DEMO?Promise.resolve({ok:true}):fetch('/api/v1/me/asks/'+encodeURIComponent(id),{method:'DELETE',credentials:'include',headers:{Accept:'application/json'}})).then(function(res){if(!res.ok){askMsg('Could not bin that — try again.','error');return;}
askData.waiting=askData.waiting.filter(function(e){return e.id!==id;});askData.answered=askData.answered.filter(function(e){return e.id!==id;});renderAsks(askData);sayAsk('Binned.');var f=$('[data-ask-input]',dash)||$('[data-cfg="settings.asks"]',dash);if(f)f.focus();}).catch(function(){askMsg('Could not reach the server.','error');}).then(function(){btn.removeAttribute('aria-busy');});}
function loadAsks(){if(!askNew||!me||!me.username)return;api('/api/v1/me/asks',null).then(function(d){if(d){askData={waiting:d.waiting||[],answered:d.answered||[]};renderAsks(askData);}}).catch(function(){});}
var nbMutual=null;function loadNb(){var name=me&&me.username,named=((cfg.settings||{}).neighbours||[]).filter(Boolean);if(!name||!named.length){nbMutual=null;return;}
if(DEMO){nbMutual=1;hydrate();return;}
api('/api/v1/neighbours/'+encodeURIComponent(name),null).then(function(d){if(d&&d.neighbours){nbMutual=d.neighbours.length;hydrate();}}).catch(function(){});}
var archBox=$('[data-arch]',dash);function archMsg(t,k){partMsg('[data-arch-msg]',t,k);}
function sayArch(t){var l=$('[data-arch-live]',dash);if(l)l.textContent=t;}
function archWhen(sec){var d=new Date(sec*1000),now=Date.now()/1000,gap=Math.max(0,now-sec);var ago=gap<90?'just now':gap<5400?Math.round(gap/60)+' minutes ago':gap<172800?Math.round(gap/3600)+' hours ago':Math.round(gap/86400)+' days ago';return{when:d.toLocaleString(undefined,{day:'numeric',month:'short',year:'numeric',hour:'numeric',minute:'2-digit'}),ago:ago};}
function renderArch(days){if(!archBox)return;if(!days||!days.length){archBox.innerHTML='<p class="dash__previewnote" style="text-align:left">nothing shelved yet — it fills up as you save.</p>';return;}
archBox.innerHTML=days.map(function(sec,i){var w=archWhen(sec);return'<div class="arch__row'+(i===0?' arch__row--now':'')+'" data-arch-at="'+sec+'">'
+'<span class="arch__when">'+esc(w.when)+'</span><span class="arch__ago">'+esc(w.ago)+(i===0?' · the one before this':'')+'</span>'
+'<button class="btn btn--ghost" type="button" data-arch-put="'+sec+'">Put this back</button>'
+(me&&me.username?'<a class="btn btn--ghost" href="/'+encodeURIComponent(me.username)+'?as='+sec+'" target="_blank" rel="noopener">Look<span class="sr-only"> at the page as it was on '+esc(w.when)+' (opens in a new tab)</span></a>':'')
+'</div>';}).join('');$$('[data-arch-put]',archBox).forEach(function(b){b.addEventListener('click',function(){archPut(b);});});}
function loadArch(){if(!archBox||!me||!me.username)return;if(DEMO){var t=Math.floor(Date.now()/1000);renderArch([t-5400,t-93000,t-260000]);return;}
api('/api/v1/profile/me/archive',null).then(function(d){if(d&&d.days)renderArch(d.days);}).catch(function(){});}
function archPut(btn){if(!btn||btn.getAttribute('aria-busy')==='true')return;var sec=btn.getAttribute('data-arch-put');if(dirty&&!confirm('You have unsaved changes. Putting an older page back will drop them. Carry on?'))return;btn.setAttribute('aria-busy','true');(DEMO?Promise.resolve({ok:false}):fetch('/api/v1/profile/me/archive/'+encodeURIComponent(sec)+'/restore',{method:'POST',credentials:'include',headers:{Accept:'application/json'}})).then(function(res){if(!res.ok){archMsg(DEMO?'The hosted preview has no account behind it — this is where your own shelf would be.':'Could not put that one back.',DEMO?'':'error');return;}
return res.json().then(function(d){if(d&&d.profile){cfg=d.profile;dirty=false;$('[data-savebar]').hidden=true;hydrate();refreshStatus(true);}
archMsg('Put back. What was here a moment ago is on the shelf, so you can undo this too.');sayArch('Page restored.');loadArch();});}).catch(function(){archMsg('Could not reach the server.','error');}).then(function(){btn.removeAttribute('aria-busy');});}
var find=$('[data-find]'),findIn=find&&$('[data-find-input]',find),findList=find&&$('[data-find-list]',find);var FIND_ALSO={'settings.vigil':'candles candle shelf burn wax vigil','settings.tally':'poll vote voting question ask visitors survey tally marks answers','settings.draw':'deck random fortune line dealt draw','settings.reverse.title':'back reverse flip other side turn over liner notes','settings.reverse.text':'back reverse flip other side turn over liner notes','settings.capsule.text':'time capsule sealed later future','settings.capsule.at':'time capsule sealed later future date','settings.sky':'weather rain snow storm fog sky','settings.remember':'returning visitor been here before memory','settings.replay':'visits last ten who came replay history','settings.asks':'ask me anything questions ama','settings.doodles':'chalkboard draw drawing chalk board','settings.guestbook':'sign signatures visitors book','settings.secret.word':'hidden link secret password easter egg','settings.usernameGlow':'gold halo glow supporter','settings.goldBorder':'gold border animated supporter','settings.sparkleTrail':'sparkle cursor trail gold supporter','settings.cursor':'pointer mouse cursor','settings.font':'typeface fonts type','settings.blocks':'content blocks clock weather quote heading','settings.shareCard.style':'share card og image preview link','profile.description':'bio about description','settings.since':'age here since anniversary wax seal old'};function findAlso(cfg){var parts=String(cfg||'').split('.');while(parts.length){var k=parts.join('.');if(FIND_ALSO[k])return FIND_ALSO[k];parts.pop();}
return'';}
var findItems=[],findPick=0;function findIndex(){var out=[],seen={};$$('.dash__sec',dash).forEach(function(sec){var secName=(($('.dash__title',sec)||{}).textContent||sec.id||'').replace(/\s+/g,' ').trim();var sub='';$$('h3,h4,[data-cfg],.toggle,[data-add-social],[data-add-block],[data-see-open],[data-vigil-snuff],[data-check-links]',sec).forEach(function(el){if(el.tagName==='H3'||el.tagName==='H4'){sub=el.textContent.replace(/\s+/g,' ').trim();out.push({label:sub,where:secName,el:el,kind:'section'});return;}
var wrap=el.closest&&el.closest('label');if(el.hasAttribute('data-cfg')&&wrap&&wrap!==el&&wrap.classList.contains('toggle'))return;var cfg=el.getAttribute('data-cfg')||'';var label='';if(el.classList.contains('toggle')){var inner=$('[data-cfg]',el);if(inner)cfg=inner.getAttribute('data-cfg')||cfg;label=el.textContent;}else if(wrap&&wrap!==el){label=wrap.textContent;}else if(el.id){var lf=sec.querySelector('label[for="'+el.id.replace(/"/g,'')+'"]');if(lf)label=lf.textContent;}
if(!label){var f=el.closest('.field'),l=f&&$('label',f);label=(l?l.textContent:el.getAttribute('aria-label')||el.textContent||'');}
label=String(label).replace(/\s+/g,' ').trim();if(!label||label.length>70)return;var key=(cfg||label)+'|'+secName;if(seen[key])return;seen[key]=1;out.push({label:label,where:sub||secName,el:el,kind:'field',also:findAlso(cfg)});});});return out;}
function findScore(it,q){var hay=(it.label+' '+it.where+' '+(it.also||'')).toLowerCase();if(!q)return it.kind==='section'?2:1;var i=it.label.toLowerCase().indexOf(q);if(i===0)return 100;if(i>0)return 80;if(hay.indexOf(q)>=0)return 50;var words=q.split(/\s+/).filter(Boolean);return words.length>1&&words.every(function(w){return hay.indexOf(w)>=0;})?30:0;}
function findMark(text,q){if(!q)return esc(text);var i=text.toLowerCase().indexOf(q);if(i<0)return esc(text);return esc(text.slice(0,i))+'<mark>'+esc(text.slice(i,i+q.length))+'</mark>'+esc(text.slice(i+q.length));}
function findRender(){var q=(findIn.value||'').trim().toLowerCase();findItems=findIndex().map(function(it){return{it:it,s:findScore(it,q)};}).filter(function(r){return r.s>0;}).sort(function(a,b){return b.s-a.s;}).slice(0,40).map(function(r){return r.it;});findPick=0;if(!findItems.length){findList.innerHTML='<li class="find__empty">nothing called that. try “candles”, “gold”, “bio”.</li>';}
else findList.innerHTML=findItems.map(function(it,i){return'<li class="find__item" role="option" id="find-o'+i+'" aria-selected="'+(i===0)+'" data-find-i="'+i+'">'
+'<b>'+findMark(it.label,q)+'</b><span>'+esc(it.where)+'</span>'
+(it.kind==='section'?'<small>section</small>':'')+'</li>';}).join('');var c=$('[data-find-count]',find);if(c)c.textContent=findItems.length?findItems.length+(findItems.length===1?' setting':' settings'):'';findSelect(0,true);}
function findSelect(i,quiet){if(!findItems.length)return;findPick=Math.max(0,Math.min(findItems.length-1,i));$$('.find__item',findList).forEach(function(el,n){el.setAttribute('aria-selected',n===findPick?'true':'false');});var el=$$('.find__item',findList)[findPick];if(el){findIn.setAttribute('aria-activedescendant',el.id);if(!quiet)el.scrollIntoView({block:'nearest'});}}
function findGo(){var it=findItems[findPick];if(!it)return;findClose();var target=it.el,box=target.closest('.field')||target;box.scrollIntoView({block:'center',behavior:'smooth'});box.classList.remove('is-found');void box.offsetWidth;box.classList.add('is-found');setTimeout(function(){box.classList.remove('is-found');},3200);var focusable=target.matches&&target.matches('input,select,textarea,button')?target:$('input,select,textarea,button',target)||target;if(focusable&&focusable.focus)setTimeout(function(){focusable.focus({preventScroll:true});},260);}
var findReturn=null;function findOpen(from){if(!find)return;findReturn=from||document.activeElement;find.hidden=false;document.body.style.overflow='hidden';findIn.value='';findRender();findIn.focus();}
function findClose(){if(!find||find.hidden)return;find.hidden=true;document.body.style.overflow='';if(findReturn&&findReturn.focus)findReturn.focus();}
$$('[data-find-open]').forEach(function(b){b.addEventListener('click',function(){findOpen(b);});});if(find){findIn.addEventListener('input',findRender);findList.addEventListener('click',function(e){var li=e.target.closest('[data-find-i]');if(!li)return;findSelect(+li.getAttribute('data-find-i'),true);findGo();});find.addEventListener('click',function(e){if(e.target===find)findClose();});findIn.addEventListener('keydown',function(e){if(e.key==='ArrowDown'){e.preventDefault();findSelect(findPick+1);}
else if(e.key==='ArrowUp'){e.preventDefault();findSelect(findPick-1);}
else if(e.key==='Enter'){e.preventDefault();findGo();}
else if(e.key==='Escape'){e.preventDefault();findClose();}});addEventListener('keydown',function(e){if(!find.hidden)return;var t=e.target,typing=t&&(t.tagName==='INPUT'||t.tagName==='TEXTAREA'||t.tagName==='SELECT'||t.isContentEditable);if((e.key==='k'||e.key==='K')&&(e.metaKey||e.ctrlKey)){e.preventDefault();findOpen(t);}
else if(e.key==='/'&&!typing&&!e.metaKey&&!e.ctrlKey&&!e.altKey){e.preventDefault();findOpen(t);}});window.__find=function(){return{open:!find.hidden,count:findItems.length,indexed:findIndex().length,first:findItems.length?findItems[0].label:null,pick:findPick,selected:($('.find__item[aria-selected="true"]',findList)||{}).textContent||''};};}
var see=$('[data-see]'),seeFrame=see&&$('[data-see-frame]',see),seeNote=see&&$('[data-see-note]',see),seeLast=null,seeBusy=false;function seeSay(html){if(seeNote)seeNote.innerHTML=html;}
function seeWidth(w){if(!seeFrame)return;seeFrame.style.width=w+'px';$$('[data-see-w]',see).forEach(function(b){b.setAttribute('aria-pressed',b.getAttribute('data-see-w')===String(w)?'true':'false');});}
function seeDraw(html){if(!seeFrame)return;seeFrame.removeAttribute('src');seeFrame.srcdoc=html;}
function seeLoad(){if(!see||seeBusy)return;seeBusy=true;see.classList.add('is-loading');seeSay('drawing your page…');if(DEMO){seeBusy=false;see.classList.remove('is-loading');seeDraw('<!doctype html><meta charset=utf-8><body style="margin:0;background:#050606;color:#A9A6A1;font:15px/1.6 Inter,system-ui,sans-serif;display:grid;place-items:center;height:100vh;text-align:center;padding:24px"><p>This is the hosted preview — there\'s no account behind it.<br>On the real dashboard this frame holds <b style="color:#F00646">your actual page</b>, drawn by the same code that serves it.</p>');seeSay('hosted preview — the real dashboard draws your own page here.');return;}
fetch('/api/v1/profile/me/preview',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json',Accept:'application/json'},body:JSON.stringify(cfg)}).then(function(r){return r.json().catch(function(){return{};}).then(function(d){if(!r.ok||!d.html){seeSay(typeof d.detail==='string'?esc(d.detail):'could not draw it just now.');return;}
seeLast=d.html;seeDraw(d.html);seeSay(d.cleared&&d.cleared.length?'nothing is saved by looking. <b>'+d.cleared.length+(d.cleared.length===1?' setting is':' settings are')+' not on your plan</b>, so the page above is drawn without '+(d.cleared.length===1?'it':'them')+'.':'nothing is saved by looking — this is the page the server would send.');});}).catch(function(){seeSay('could not reach the server.');}).then(function(){seeBusy=false;see.classList.remove('is-loading');});}
var seeReturn=null;function seeOpen(from){if(!see)return;seeReturn=from||document.activeElement;see.hidden=false;document.body.style.overflow='hidden';seeWidth(390);seeLoad();var c=$('[data-see-close]',see);if(c)c.focus();}
function seeClose(){if(!see||see.hidden)return;see.hidden=true;document.body.style.overflow='';if(seeFrame){seeFrame.srcdoc='';}if(seeReturn&&seeReturn.focus)seeReturn.focus();}
$$('[data-see-open]').forEach(function(b){b.addEventListener('click',function(){seeOpen(b);});});if(see){see.addEventListener('click',function(e){if(e.target===see)seeClose();});var sc=$('[data-see-close]',see);if(sc)sc.addEventListener('click',seeClose);var sa=$('[data-see-again]',see);if(sa)sa.addEventListener('click',seeLoad);$$('[data-see-w]',see).forEach(function(b){b.addEventListener('click',function(){seeWidth(+b.getAttribute('data-see-w'));});});addEventListener('keydown',function(e){if(e.key==='Escape'&&!see.hidden)seeClose();if(e.key==='Tab'&&!see.hidden){var f=$$('button,[href],[tabindex="0"]',see).filter(function(el){return el.offsetParent!==null;});if(!f.length)return;var first=f[0],last=f[f.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus();}
else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus();}}});window.__see=function(){return{open:!see.hidden,width:seeFrame?seeFrame.style.width:'',note:(seeNote||{}).textContent||'',drawn:!!seeLast};};}
var vigilLit=null,vgSnuff=$('[data-vigil-snuff]',dash);function vigilMsg(text,kind){partMsg('[data-vigil-msg]',text,kind);}
function loadVigil(){var name=me&&me.username,on=String(((cfg.settings||{}).vigil)||'').trim();if(!name||['6','12','24'].indexOf(on)<0){vigilLit=null;hydrate();return;}
api('/api/v1/vigil/'+encodeURIComponent(name),null).then(function(d){if(d&&d.lit){vigilLit=d.lit.length;hydrate();}}).catch(function(){});}
if(vgSnuff)vgSnuff.addEventListener('click',function(){if(vgSnuff.getAttribute('aria-busy')==='true')return;vgSnuff.setAttribute('aria-busy','true');(DEMO?Promise.resolve({ok:true}):fetch('/api/v1/me/vigil',{method:'DELETE',credentials:'include',headers:{Accept:'application/json'}})).then(function(res){if(!res.ok){vigilMsg('Could not put them out — try again.','error');return;}
vigilLit=0;hydrate();vigilMsg('Shelf cleared — the next visitor starts it again.');}).catch(function(){vigilMsg('Could not reach the server.','error');}).then(function(){vgSnuff.removeAttribute('aria-busy');});});$$('input.field__input',dash).forEach(function(i){if(!i.form&&!i.hasAttribute('enterkeyhint'))i.setAttribute('enterkeyhint','done');});dash.addEventListener('focusin',function(e){var bar=$('[data-savebar]');if(!bar||bar.hidden)return;var el=e.target;if(!el||!el.closest||el.closest('[data-savebar]'))return;var box=el.closest('.field,.social-row,.block-row')||el;var r=box.getBoundingClientRect(),b=bar.getBoundingClientRect();var over=r.bottom-b.top+12;if(over>0)scrollBy({top:Math.min(over,Math.max(0,r.top-8)),behavior:'auto'});});dash.addEventListener('keydown',function(e){var el=e.target;if(e.key!=='Enter'||!el||el.tagName!=='INPUT'||el.form||!el.classList.contains('field__input'))return;if(window.matchMedia&&matchMedia('(hover:none)').matches){e.preventDefault();el.blur();}});var badgesEl=$('[data-badges]',dash);function badgesEmptyCopy(){var r=rank(me&&me.plan),pricing=(DEMO&&DEMO.pricing)||'/pricing';if(r>=2)return'No badges on this account yet. Supporter comes with the gold check and tag — they’re granted by the team rather than switched on here, so if yours isn’t showing, it hasn’t been applied yet.';if(r===1)return'No badges on this account yet. Lifetime comes with the verified check — it’s granted by the team rather than switched on here, so if yours isn’t showing, it hasn’t been applied yet.';return'No badges on this account yet. Badges are granted, not toggled — the verified check comes with <a href="'+pricing+'#lifetime">Lifetime</a>, the gold one with <a href="'+pricing+'#supporter">Supporter</a>.';}
function renderBadges(){if(!badgesEl)return;var list=(me&&me.badges)||[];badgesEl.innerHTML=list.length?list.map(function(b){return'<label class="badge-row"><span class="badge-row__dot" style="--bc:'+esc(String(b.color||'#3b82f6'))+'">'+ic('check')+'</span><span class="badge-row__info"><b>'+esc(String(b.name||b.id))+'</b><span>'+esc(String(b.description||''))+'</span></span><span class="toggle" data-toggle><input type="checkbox" data-badge="'+esc(String(b.id))+'" aria-label="Show the '+esc(String(b.name||b.id))+' badge on your page"'+(b.enabled!==false?' checked':'')+'><i aria-hidden="true"></i>show</span></label>';}).join(''):'<p class="dash__previewnote badges__empty">'+badgesEmptyCopy()+'</p>';var badgesBox=badgesEl.parentElement;$$('[data-badge]',badgesEl).forEach(function(inp){inp.addEventListener('change',function(){if(inp.getAttribute('aria-busy')==='true'){inp.checked=!inp.checked;return;}var id=inp.dataset.badge,on=inp.checked;inp.setAttribute('aria-busy','true');var note=$('[data-badges-msg]',badgesBox);if(note){note.hidden=true;note.classList.remove('is-visible');}
(DEMO?Promise.resolve({ok:true}):fetch('/api/v1/me/badges/'+encodeURIComponent(id),{method:'PATCH',credentials:'include',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify({enabled:on})})).then(function(r){if(!r.ok){inp.checked=!on;formMsg(badgesBox,'Could not update that badge — it’s back the way it was.','error');return;}(me.badges||[]).forEach(function(b){if(String(b.id)===id)b.enabled=on;});cfg.badges=(me.badges||[]).map(function(b){return{id:b.id,name:b.name,description:b.description,color:b.color,owned:true,enabled:b.enabled!==false};});preview();stats();}).catch(function(){inp.checked=!on;formMsg(badgesBox,'Could not reach the server — the badge is back the way it was.','error');}).then(function(){inp.removeAttribute('aria-busy');});});});}
function msg(t,kind){if(window.showAuthMessage)showAuthMessage(t,kind);}
function hideMsg(){var box=$('#auth-message');if(box){box.hidden=true;box.classList.remove('is-visible');}}
function formMsg(form,t,kind){var box=form&&$('[data-form-msg]',form);if(!box){msg(t,kind);return;}box.hidden=false;box.textContent=t;box.classList.add('is-visible');box.classList.toggle('auth-message--error',kind==='error');box.classList.toggle('auth-message--ok',kind!=='error');}
function markBad(el,bad){if(!el)return;if(bad){el.setAttribute('aria-invalid','true');el.focus();if(!el.__unbad){el.__unbad=true;el.addEventListener('input',function(){el.removeAttribute('aria-invalid');});}}else el.removeAttribute('aria-invalid');}
var EDITABLE=['profile','settings','assets','socials'];function editable(c){c=c||{};var p=c.profile||{};return{profile:{displayName:p.displayName||'',description:p.description||'',location:p.location||''},settings:c.settings||{},assets:c.assets||{},socials:c.socials||[]};}
function draftKey(){return me&&me.id?'misaDraft:'+me.id:null;}
var draftTimer=null;function stash(){var k=draftKey();if(!k)return;clearTimeout(draftTimer);draftTimer=setTimeout(function(){try{localStorage.setItem(k,JSON.stringify({at:Date.now(),cfg:editable(cfg)}));}catch(e){}},400);}
function unstash(){var k=draftKey();if(!k)return;clearTimeout(draftTimer);try{localStorage.removeItem(k);}catch(e){}}
function readDraft(){var k=draftKey();if(!k)return null;try{var d=JSON.parse(localStorage.getItem(k)||'null');return d&&d.cfg?d:null;}catch(e){return null;}}
var draftBar=$('[data-draft]',dash);function when(t){var d=new Date(t),now=new Date();var time=d.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});return d.toDateString()===now.toDateString()?'today at '+time:d.toLocaleDateString()+' at '+time;}
function showDraft(d){if(!draftBar)return;draftBar.hidden=false;draftBar.dataset.mode='restore';var rl0=$('[data-draft-reload]',draftBar);if(rl0)rl0.hidden=true;$('[data-draft-text]',draftBar).textContent='You have unsaved changes from '+when(d.at)+' on this device.';$('[data-draft-restore]',draftBar).hidden=false;$('[data-draft-discard]',draftBar).hidden=false;$('[data-draft-login]',draftBar).hidden=true;}
function showExpired(){if(!draftBar)return;draftBar.hidden=false;draftBar.dataset.mode='expired';var rl1=$('[data-draft-reload]',draftBar);if(rl1)rl1.hidden=true;$('[data-draft-text]',draftBar).textContent='Your session expired. Your changes are kept on this device — log in again and they\u2019ll be waiting here.';$('[data-draft-restore]',draftBar).hidden=true;$('[data-draft-discard]',draftBar).hidden=true;$('[data-draft-login]',draftBar).hidden=false;}
function hideDraft(){if(draftBar)draftBar.hidden=true;}
function savedKey(){return me&&me.id?'misaSaved:'+me.id:null;}
function showStale(){if(!draftBar||draftBar.dataset.mode==='expired')return;draftBar.hidden=false;draftBar.dataset.mode='stale';$('[data-draft-text]',draftBar).textContent='This page was just saved from another tab or device. Reload to see those changes — saving here would overwrite them.';$('[data-draft-restore]',draftBar).hidden=true;$('[data-draft-discard]',draftBar).hidden=true;$('[data-draft-login]',draftBar).hidden=true;var rl=$('[data-draft-reload]',draftBar);if(rl)rl.hidden=false;}
addEventListener('storage',function(e){var k=savedKey();if(k&&e.key===k&&e.newValue)showStale();});if(draftBar){var rlBtn=$('[data-draft-reload]',draftBar);if(rlBtn)rlBtn.addEventListener('click',function(){dirty=false;location.reload();});}
function restoreDraft(d){var e=d.cfg;cfg.profile=Object.assign({},cfg.profile,e.profile);cfg.settings=Object.assign({},cfg.settings,e.settings);cfg.assets=Object.assign({},cfg.assets,e.assets);cfg.socials=e.socials||cfg.socials;hydrate();touch();hideDraft();msg('Restored your unsaved changes — hit save to publish them.','ok');}
if(draftBar){$('[data-draft-restore]',draftBar).addEventListener('click',function(){var d=readDraft();if(d)restoreDraft(d);else hideDraft();});$('[data-draft-discard]',draftBar).addEventListener('click',function(){unstash();hideDraft();});}
var saveBtns=$$('[data-save]',dash.parentElement);function setLink(u){$$('[data-link-user]').forEach(function(el){el.textContent=u||'…';});$$('[data-view-page]').forEach(function(a){a.href=u?'/'+u:'/';});}
var DEMO=window.MISA_DEMO;function api(url,fallback){if(DEMO)return Promise.resolve(DEMO[url]||fallback);return fetch(url,{credentials:'include',headers:{Accept:'application/json'}}).then(function(r){return r.ok?r.json():fallback;});}
function setLoading(on){dash.classList.toggle('is-loading',on);dash.setAttribute('aria-busy',on?'true':'false');fields().forEach(function(el){el.disabled=on;});$$('[data-add-social],[data-theme-pick],[data-theme-reset]',dash).forEach(function(b){b.disabled=on;});saveBtns.forEach(function(b){b.disabled=on;});}
function load(){setLoading(true);var pMe=api('/api/v1/me',null),pProfile=api('/api/v1/profile/me',{profile:null}),pStats=api('/api/v1/me/stats',null);pProfile.catch(function(){});pStats.catch(function(){});pMe.then(function(user){me=user;if(user&&user.username)setLink(user.username);else{setLink('');status('no username','pick one under Account to publish',false);}
return pProfile;}).then(function(data){var saved=data&&data.profile;var base=defaults(me&&me.username||'');if(saved){cfg={};Object.keys(base).forEach(function(k){cfg[k]=Object.assign({},base[k],saved[k]||{});});cfg.socials=saved.socials||[];cfg.badges=saved.badges||[];cfg.assets=Object.assign({},base.assets,saved.assets||{});status('live','your page is published',true);}
else{cfg=base;if(me&&me.username)status('not published','fill in your page and hit save',false);var vn0=$('[data-stat-views-note]');if(vn0)vn0.textContent='once your page is live';}
var picked=themeFromSignup();if(picked&&!saved){cfg.settings=Object.assign({},cfg.settings,PRESETS[picked]);}
if(me){cfg.profile.username=me.username||'';if(!cfg.profile.displayName)cfg.profile.displayName=me.display_name||me.username||'';cfg.profile.uid=me.id;if(me.badges)cfg.badges=me.badges.map(function(b){return{id:b.id,name:b.name,description:b.description,color:b.color,owned:true,enabled:b.enabled!==false};});}
setLoading(false);applyPlan();applyUploads();applyDelete();applyShare();renderBadges();hydrate();dirty=false;$('[data-savebar]').hidden=true;checklist(true);syncTheme();if(me)pStats.then(renderStats).catch(function(){if(ana)ana.hidden=true;});else if(ana)ana.hidden=true;loadBook();loadBoard();loadReplay();loadAsks();loadVigil();loadArch();loadNb();if(uIn&&uBtn){var uv=uIn.value.trim(),mine=uv.toLowerCase()===(me&&me.username||'').toLowerCase();uBtn.disabled=!uv||mine;if(uv&&mine&&uHint&&/save to make it yours/.test(uHint.textContent))uState(null,'that’s your name now');}
var draft=readDraft();if(draft&&JSON.stringify(draft.cfg)!==JSON.stringify(editable(cfg)))showDraft(draft);else if(draft)unstash();if(picked&&!saved){lastTheme=picked;touch();msg('Starting from the “'+picked+'” theme you picked — hit save when it looks right.','ok');}}).catch(function(){loadFailed=true;dash.classList.remove('is-loading');dash.setAttribute('aria-busy','false');refreshStatus();msg('Could not load your page. Refresh to try again.','error');});}
var sbar=$('[data-savebar]'),sbarText=sbar&&$('[data-savebar-text]',sbar),sbarBtn=sbar&&$('[data-savebar-btn]',sbar);var toastTimer=null,sbarOpen=sbar&&$('[data-savebar-open]',sbar);function unsaved(){if(!sbar)return;clearTimeout(toastTimer);sbar.classList.remove('is-saved');if(sbarText)sbarText.setAttribute('aria-live','polite');if(sbarOpen)sbarOpen.hidden=true;}
function showSaved(){if(!sbar)return;var hadFocus=sbar.contains(document.activeElement);unsaved();sbar.classList.remove('is-failed');sbar.classList.add('is-saved');if(sbarBtn)sbarBtn.textContent='Save changes';if(sbarText){sbarText.setAttribute('aria-live','off');sbarText.innerHTML='Saved<span class="dash__saved-more"> — your page is up to date</span>';}if(sbarOpen)sbarOpen.hidden=false;sbar.hidden=false;if(hadFocus&&sbarOpen)sbarOpen.focus();toastTimer=setTimeout(hideToast,4000);}
function hideToast(){if(!sbar||!sbar.classList.contains('is-saved'))return;var fv=false;try{fv=sbar.contains(document.activeElement)&&document.activeElement.matches(':focus-visible');}catch(e){}
if(fv){sbar.addEventListener('focusout',function once(){sbar.removeEventListener('focusout',once);setTimeout(hideToast,60);});return;}
sbar.hidden=true;unsaved();}
var expiredBar=false;function saveState(fail){if(!sbar)return;unsaved();expiredBar=fail==='session expired';sbar.classList.toggle('is-failed',!!fail);if(sbarText)sbarText.textContent=fail?fail:'unsaved changes';if(sbarBtn)sbarBtn.textContent=expiredBar?'Log in again':fail?'Try again':'Save changes';if(fail){sbar.hidden=false;if(!reduce){sbar.classList.remove('is-shake');void sbar.offsetWidth;sbar.classList.add('is-shake');}}}
var saving=false;function save(){if(saving)return;if(!me||!me.username){msg('Pick a username first (under Account) — that’s your link.','error');location.hash='#account';return;}
cfg.profile.username=me.username;if(!cfg.profile.displayName)cfg.profile.displayName=me.username;saving=true;saveBtns.forEach(function(b){b.setAttribute('aria-busy','true');});saveState(null);if(sbarText)sbarText.textContent='saving…';if(sbarBtn)sbarBtn.textContent='Saving…';(DEMO?new Promise(function(res){setTimeout(function(){res({ok:true,d:{profile:cfg}});},350);}):fetch('/api/v1/profile/me',{method:'PUT',credentials:'include',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify(cfg)}).then(function(r){return r.json().catch(function(){return{};}).then(function(d){return{ok:r.ok,status:r.status,d:d};});})).then(function(res){if(!res.ok){if(res.status===401){clearTimeout(draftTimer);try{var k=draftKey();if(k)localStorage.setItem(k,JSON.stringify({at:Date.now(),cfg:editable(cfg)}));}catch(e){}hideMsg();showExpired();saveState('session expired');var lg=draftBar&&$('[data-draft-login]',draftBar);if(lg)lg.focus();return;}var why=res.status>=500?'Couldn’t save — something broke on our side. Your changes are still here; try again in a moment.':((res.d&&(typeof res.d.detail==='string'?res.d.detail:''))||(res.status===413?'That’s too big to save — a shorter bio or fewer links.':'Could not save.'));msg(why,'error');saveState(res.status===413?'too big to save':res.status>=500?'couldn’t save — try again':'couldn’t save');return;}if(res.d.profile)cfg=res.d.profile;dirty=false;showSaved();published=true;refreshStatus(true);applyShare();checklist(true);unstash();hideDraft();loadReplay();try{var sk=savedKey();if(sk)localStorage.setItem(sk,String(Date.now()));}catch(e){}
var cl=res.d.cleared||[];msg(cl.length?'Saved — '+cl.length+' paid-only setting'+(cl.length>1?'s were':' was')+' left off (see the plan box).':'Saved. misa.lol/'+me.username+' is up to date.',cl.length?'error':'ok');hydrate();}).catch(function(){msg('Could not reach the server.','error');saveState('couldn’t reach the server');}).then(function(){saving=false;saveBtns.forEach(function(b){b.removeAttribute('aria-busy');});});}
saveBtns.forEach(function(b){b.addEventListener('click',function(){if(expiredBar&&sbar&&sbar.contains(b)){var lg0=draftBar&&$('[data-draft-login]',draftBar);location.href=lg0&&lg0.getAttribute('href')||'/login';return;}save();});});$$('[data-view-page]').forEach(function(a){a.addEventListener('click',function(e){if(!me||!me.username){e.preventDefault();msg('Pick a username first (under Account) — that’s your link.','error');location.hash='#account';return;}
if(!published){e.preventDefault();msg('Your page isn’t live yet — hit save first.','error');return;}
if(dirty)msg('Opened what’s saved — your unsaved changes aren’t on it yet.','ok');});});var peek=$('[data-peek]'),sheet=$('#dash-preview');function setPeek(open){if(!sheet)return;sheet.classList.toggle('is-open',open);document.body.classList.toggle('is-peeking',open);if(peek)peek.setAttribute('aria-expanded',String(open));if(open){sheet.setAttribute('role','dialog');sheet.setAttribute('aria-modal','true');}else{sheet.removeAttribute('role');sheet.removeAttribute('aria-modal');}
$$('.dash > *').forEach(function(el){if(el!==sheet){if(open)el.setAttribute('inert','');else el.removeAttribute('inert');}});if(open){var c=$('[data-peek-close]',sheet);if(c)c.focus();}else if(peek)peek.focus();}
function peekTrap(e){if(e.key!=='Tab'||!sheet.classList.contains('is-open'))return;var f=$$('button,a[href],input,select,textarea,[tabindex]:not([tabindex="-1"])',sheet).filter(function(el){return!el.disabled&&el.offsetParent!==null;});if(!f.length)return;var first=f[0],last=f[f.length-1];if(f.length===1||!sheet.contains(document.activeElement)){e.preventDefault();first.focus();return;}if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus();}}
if(peek&&sheet){peek.addEventListener('click',function(){setPeek(!sheet.classList.contains('is-open'));});$('[data-peek-close]',sheet).addEventListener('click',function(){setPeek(false);});sheet.addEventListener('click',function(e){if(e.target===sheet)setPeek(false);});addEventListener('keydown',function(e){if(e.key==='Escape'&&sheet.classList.contains('is-open'))setPeek(false);});addEventListener('keydown',peekTrap);matchMedia('(min-width:1241px)').addEventListener('change',function(m){if(m.matches)setPeek(false);});}
var leaving=false;addEventListener('beforeunload',function(e){if(dirty&&!leaving){e.preventDefault();e.returnValue='';}});var logoutBtn=$('#logout-btn');if(logoutBtn)logoutBtn.addEventListener('click',function(e){if(!dirty)return;try{var k=draftKey();if(k)localStorage.setItem(k,JSON.stringify({at:Date.now(),cfg:editable(cfg)}));}catch(err){}if(confirm('You have unsaved changes. They’ll be kept on this device for next time — log out anyway?')){leaving=true;}else{e.stopImmediatePropagation();e.preventDefault();}},true);var dashLive=null;function sayDash(t){if(!dashLive){dashLive=document.createElement('p');dashLive.className='sr-only';dashLive.setAttribute('aria-live','polite');dashLive.setAttribute('data-dash-live','');dash.appendChild(dashLive);}dashLive.textContent='';setTimeout(function(){dashLive.textContent=t;},30);}
function copyText(t){if(navigator.clipboard&&navigator.clipboard.writeText)return navigator.clipboard.writeText(t);return new Promise(function(res,rej){var ta=document.createElement('textarea');ta.value=t;ta.setAttribute('readonly','');ta.style.position='fixed';ta.style.left='-9999px';document.body.appendChild(ta);ta.select();var okc=false;try{okc=document.execCommand('copy');}catch(e){}document.body.removeChild(ta);okc?res():rej(new Error('copy'));});}
var msCopy=$('[data-ms-copy]',dash);if(msCopy)msCopy.addEventListener('click',function(){var box=$('[data-milestone]',dash),link=box&&box.dataset.url;if(!link)return;var b=this;copyText(link).then(function(){b.textContent='Copied';sayDash('Copied the milestone card’s link.');setTimeout(function(){b.textContent='Copy its link';},2200);},function(){b.textContent='Copy failed';setTimeout(function(){b.textContent='Copy its link';},2200);});});$('[data-copy-link]').addEventListener('click',function(){var u=me&&me.username;if(!u)return;var b=this,link='https://misa.lol/'+u;copyText(link).then(function(){b.textContent='Copied';sayDash('Copied '+link.replace(/^https:\/\//,'')+'.');},function(){b.textContent='Copy failed';sayDash('Couldn’t copy — your link is '+link.replace(/^https:\/\//,'')+'.');}).then(function(){setTimeout(function(){b.textContent='Copy';},1600);});});var delForm=$('[data-delete-form]',dash);if(delForm)delForm.addEventListener('submit',function(e){e.preventDefault();var pw=$('[data-delete-password]',delForm);if(delForm.confirm.value.trim().toLowerCase()!=='delete'){formMsg(delForm,'Type delete to confirm.','error');markBad(delForm.confirm,true);return;}
if(!confirm('Delete your account and your page? This cannot be undone.'))return;var btn=$('[type=submit]',delForm);btn.disabled=true;(DEMO?new Promise(function(res){setTimeout(function(){res({ok:false,d:{detail:'Demo: nothing was deleted.'}});},400);}):fetch('/api/v1/me',{method:'DELETE',credentials:'include',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify({confirm:'delete',password:(pw&&!pw.hidden)?delForm.password.value:null})}).then(function(r){return r.json().catch(function(){return{};}).then(function(d){return{ok:r.ok,d:d};});})).then(function(res){if(!res.ok){formMsg(delForm,(res.d&&typeof res.d.detail==='string')?res.d.detail:'Could not delete the account.','error');btn.disabled=false;return;}dirty=false;location.href=res.d.redirect||'/';}).catch(function(){formMsg(delForm,'Could not reach the server.','error');btn.disabled=false;});});function applyDelete(){var pw=$('[data-delete-password]',dash);if(pw)pw.hidden=!(me&&me.providers&&me.providers.email);var has=!!(me&&me.providers&&me.providers.email),cur=$('[data-password-current]',dash),ttl=$('[data-password-title]',dash),note=$('[data-password-note]',dash);if(cur)cur.hidden=!has;if(ttl)ttl.textContent=has?'Change password':'Set a password';if(note)note.textContent=has?'Pick something long. Sessions elsewhere stay signed in.':'You signed up with Google, Discord or Telegram — a password lets you log in with your email too.';}
var pwForm=$('[data-password-form]',dash);if(pwForm)pwForm.addEventListener('submit',function(e){e.preventDefault();if(pwForm.new_password.value!==pwForm.again.value){formMsg(pwForm,'The two passwords don’t match.','error');markBad(pwForm.again,true);return;}
var btn=$('[type=submit]',pwForm);btn.disabled=true;var has=!!(me&&me.providers&&me.providers.email);(DEMO?new Promise(function(res){setTimeout(function(){res({ok:true,d:{}});},400);}):fetch('/api/v1/me/password',{method:'PATCH',credentials:'include',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify({current_password:has?pwForm.current_password.value:null,new_password:pwForm.new_password.value})}).then(function(r){return r.json().catch(function(){return{};}).then(function(d){return{ok:r.ok,d:d};});})).then(function(res){if(!res.ok){formMsg(pwForm,(res.d&&typeof res.d.detail==='string')?res.d.detail:'Could not save the password.','error');return;}formMsg(pwForm,has?'Password changed.':'Password set — you can log in with your email now.','ok');pwForm.reset();if(!has&&!DEMO)setTimeout(function(){location.reload();},900);}).catch(function(){formMsg(pwForm,'Could not reach the server.','error');}).then(function(){btn.disabled=false;});});var uf=$('#username-form');if(uf)uf.addEventListener('submit',function(){if(window.showAuthMessage&&!window.__misaUsernameHook){var orig=window.showAuthMessage,done=false;window.__misaUsernameHook=true;var restore=function(){if(done)return;done=true;window.showAuthMessage=orig;window.__misaUsernameHook=false;};window.showAuthMessage=function(t,k){restore();formMsg(uf,t,k);};setTimeout(restore,8000);}
setTimeout(load,900);});var uIn=uf&&$('#username',uf),uHint=uf&&$('[data-username-hint]',uf),uBtn=uf&&$('[type="submit"]',uf),uField=uIn&&uIn.closest('.field'),uBase=uHint&&uHint.textContent;function uState(state,text){if(!uField)return;uField.classList.remove('is-valid','is-invalid');if(state)uField.classList.add(state==='ok'?'is-valid':'is-invalid');if(uHint)uHint.innerHTML=text;}
if(uIn)uIn.addEventListener('input',function(){var v=uIn.value.trim(),cur=(me&&me.username||'').toLowerCase();if(!v){uState(null,uBase);if(uBtn)uBtn.disabled=true;return;}
if(!RE.test(v)){uState('bad','3–24 characters, starts with a letter — letters, numbers, underscores');if(uBtn)uBtn.disabled=true;return;}
if(v.toLowerCase()===cur){uState(null,'that’s your name already');if(uBtn)uBtn.disabled=true;return;}
if(uBtn)uBtn.disabled=false;if(!ON_SITE||!window.fetch){uState(null,'misa.lol/<b>'+esc(v)+'</b> — save to make it yours');return;}
uState(null,'checking misa.lol/<b>'+esc(v)+'</b>…');checkAvail(v.toLowerCase(),function(res){if(uIn.value.trim().toLowerCase()!==v.toLowerCase())return;if(!res||res.available==null)return;if(res.available)uState('ok','misa.lol/<b>'+esc(v)+'</b> is free — save to make it yours');else{uState('bad',availHint(v,res));if(uBtn)uBtn.disabled=true;}
if(uHint)sayDash(uHint.textContent.replace(/\s+/g,' ').trim());});});var meRefreshing=false;function refreshMe(){if(meRefreshing||!me||DEMO)return;meRefreshing=true;api('/api/v1/me',null).then(function(user){if(!user)return;var was=(me.plan||'free').toLowerCase(),now=(user.plan||'free').toLowerCase();var hadBadges=(me.badges||[]).map(function(b){return b.id;}).join(','),hasBadges=(user.badges||[]).map(function(b){return b.id;}).join(',');me=user;if(was===now&&hadBadges===hasBadges)return;applyPlan();applyUploads();applyDelete();applyShare();renderBadges();syncTheme();var label=now==='supporter'?'Supporter':now==='lifetime'?'Lifetime':'Free';if(was!==now){msg(rank(now)>rank(was)?label+' is on — the perks below are unlocked.':'Your plan is now '+label+'.',rank(now)>rank(was)?'ok':'error');}
else msg('A badge was added to your account — it’s in the Badges list below.','ok');}).catch(function(){}).then(function(){meRefreshing=false;});}
document.addEventListener('visibilitychange',function(){if(!document.hidden)refreshMe();});addEventListener('pageshow',function(e){if(e.persisted)refreshMe();});var dnav=$$('.dash__nav a'),dsecs=$$('.dash__sec');if('IntersectionObserver'in window){var dspy=new IntersectionObserver(function(en){en.forEach(function(x){if(x.isIntersecting)dnav.forEach(function(a){var on=a.getAttribute('href')==='#'+x.target.id;a.classList.toggle('is-active',on);if(on)a.setAttribute('aria-current','true');else a.removeAttribute('aria-current');});});},{rootMargin:'-15% 0px -70% 0px'});dsecs.forEach(function(s){dspy.observe(s);});}
load();}
var adm=$('.adm-editor');if(adm){var ADEMO=window.MISA_DEMO_ADMIN,A='/api/v1/admin',selected=null,badgeList=[],userOffset=0,userQuery='';function amsg(t,kind){if(window.showAuthMessage)showAuthMessage(t,kind);else{var m=$('#auth-message');if(m){m.textContent=t;m.className='auth-message is-visible '+(kind==='error'?'auth-message--error':'auth-message--ok');}}}
function call(method,path,body){if(ADEMO){return Promise.resolve(ADEMO.handle(method,path,body));}
return fetch(A+path,{method:method,credentials:'include',headers:Object.assign({Accept:'application/json'},body?{'Content-Type':'application/json'}:{}),body:body?JSON.stringify(body):undefined}).then(function(r){return r.json().catch(function(){return{};}).then(function(d){if(!r.ok)throw new Error((d&&typeof d.detail==='string')?d.detail:'Request failed ('+r.status+')');return d;});});}
function when(v){if(!v)return'—';var d=new Date(v);return isNaN(d)?String(v):d.toLocaleDateString(undefined,{day:'numeric',month:'short'})+' '+d.toLocaleTimeString(undefined,{hour:'2-digit',minute:'2-digit'});}
function short(id){return id?String(id).slice(0,8):'—';}
function who(id){var u=lastUsers.filter(function(x){return x.id===id;})[0];return u?'<b>'+esc(u.username||u.email||short(id))+'</b>':'<code>'+esc(short(id))+'…</code>';}
function table(cols,rows,empty){if(!rows.length)return'<p class="adm-empty">'+esc(empty)+'</p>';return'<div class="adm-scroll"><table><thead><tr>'+cols.map(function(c){return'<th scope="col">'+(c?esc(c):'<span class="sr-only">actions</span>')+'</th>';}).join('')+'</tr></thead><tbody>'+rows.join('')+'</tbody></table></div>';}
function fail(el){return function(e){el.innerHTML='<p class="adm-empty">'+esc(e.message||'Could not load.')+'</p>';};}
var usersEl=$('[data-users]',adm),moreEl=$('[data-users-more]',adm),selEl=$('[data-selected]',adm),lastUsers=[];function renderUsers(list,append){var rows=list.map(function(u){var susp=!!u.suspended_at;return'<tr data-uid="'+esc(u.id)+'"'+(selected&&selected.id===u.id?' class="is-selected"':'')+'><td><b>'+esc(u.username||'—')+'</b>'+(u.is_admin?'<span class="tag">admin</span>':'')+'</td><td>'+esc(u.email||'—')+'</td><td>'+when(u.created_at)+'</td><td>'+when(u.last_login_at)+'</td><td class="'+(susp?'bad':'ok')+'">'+(susp?'suspended':'active')+'</td><td><button class="btn btn--ghost btn--sm" type="button" data-pick aria-label="Manage '+esc(u.username||u.email||u.id)+'">Manage</button></td></tr>';});var html=table(['user','email','joined','last login','status',''],rows,'no users match.');if(append&&usersEl.querySelector('tbody')){var tmp=document.createElement('div');tmp.innerHTML=html;var tb=tmp.querySelector('tbody');if(tb)usersEl.querySelector('tbody').insertAdjacentHTML('beforeend',tb.innerHTML);}else usersEl.innerHTML=html;$$('[data-pick]',usersEl).forEach(function(b){b.addEventListener('click',function(){var id=b.closest('tr').dataset.uid;select(lastUsers.filter(function(u){return u.id===id;})[0]);});});moreEl.hidden=list.length<50;}
function loadUsers(append){if(!append){userOffset=0;lastUsers=[];}call('GET','/users?search='+encodeURIComponent(userQuery)+'&limit=50&offset='+userOffset).then(function(d){lastUsers=lastUsers.concat(d.users||[]);renderUsers(d.users||[],append);userOffset+=50;}).catch(fail(usersEl));}
$('[data-user-search]',adm).addEventListener('submit',function(e){e.preventDefault();userQuery=this.q.value.trim();loadUsers(false);});$('[data-users-next]',adm).addEventListener('click',function(){loadUsers(true);});function select(u){selected=u||null;$$('tr[data-uid]',usersEl).forEach(function(tr){tr.classList.toggle('is-selected',!!u&&tr.dataset.uid===u.id);});if(!u){selEl.hidden=true;return;}
var susp=!!u.suspended_at,opts=badgeList.map(function(b){return'<option value="'+esc(b.id)+'">'+esc(b.name)+'</option>';}).join(''),who=esc(u.username||u.email||u.id);selEl.hidden=false;selEl.setAttribute('role','region');selEl.setAttribute('aria-label','Manage '+who);selEl.setAttribute('tabindex','-1');selEl.innerHTML='<div class="adm-user__head"><b>'+esc(u.display_name||u.username||'—')+'</b><span>misa.lol/'+esc(u.username||'?')+' · '+esc(u.email||'no email')+' · <code>'+esc(u.id)+'</code></span></div>'+'<div class="adm-user__row"><span class="tile__label" aria-hidden="true">plan</span><div class="field__box"><select class="field__input" data-grant-plan aria-label="Plan to grant"><option value="lifetime">Lifetime ($5)</option><option value="supporter">Supporter ($9.99)</option></select></div><div class="field__box"><input class="field__input" type="date" data-grant-until aria-label="Expires (optional)"></div><button class="btn btn--crimson btn--sm" type="button" data-grant aria-label="Grant the plan to '+who+'">Grant</button></div>'+'<div class="adm-user__row"><span class="tile__label" aria-hidden="true">badge</span><div class="field__box"><select class="field__input" data-give-badge aria-label="Badge to give">'+(opts||'<option value="">no badges yet</option>')+'</select></div><button class="btn btn--ivory btn--sm" type="button" data-give aria-label="Give the badge to '+who+'">Give</button><button class="btn btn--ghost btn--sm" type="button" data-take aria-label="Hide the badge on '+who+'’s page">Hide</button></div>'+'<div class="adm-user__row"><span class="tile__label" aria-hidden="true">account</span>'+(susp?'<span class="bad">suspended'+(u.suspension_reason?' — '+esc(u.suspension_reason):'')+(u.suspended_until?' until '+when(u.suspended_until):'')+'</span><button class="btn btn--ghost btn--sm" type="button" data-unsuspend>Lift suspension</button>':'<div class="field__box"><input class="field__input" type="text" maxlength="500" placeholder="reason" data-susp-reason aria-label="Reason for suspension (optional)"></div><button class="btn btn--ghost btn--sm" type="button" data-suspend aria-label="Suspend '+who+'">Suspend</button>')+'<a class="btn btn--ghost btn--sm" href="/'+esc(u.username||'')+'" target="_blank" rel="noopener">View page<span class="sr-only"> — opens in a new tab</span></a><button class="btn btn--ghost btn--sm danger__btn" type="button" data-delete-user aria-label="Delete '+who+'’s account">Delete account</button><button class="btn btn--ghost btn--sm" type="button" data-close>Close</button></div>';var opener=document.activeElement&&document.activeElement.hasAttribute('data-pick')?document.activeElement:null;if(opener||!selEl.contains(document.activeElement))selEl.focus({preventScroll:false});$('[data-close]',selEl).__opener=opener;$('[data-grant]',selEl).addEventListener('click',function(){var plan=$('[data-grant-plan]',selEl).value,until=$('[data-grant-until]',selEl).value;call('POST','/entitlements',{user_id:u.id,plan:plan,active:true,expires_at:until?until+'T23:59:59Z':null}).then(function(){amsg('Granted '+plan+' to '+(u.username||u.id)+'.','ok');loadEntitlements();}).catch(function(e){amsg(e.message,'error');});});$('[data-give]',selEl).addEventListener('click',function(){var id=$('[data-give-badge]',selEl).value;if(!id)return;call('PUT','/users/'+u.id+'/badges/'+encodeURIComponent(id),{enabled:true}).then(function(){amsg('Badge given.','ok');}).catch(function(e){amsg(e.message,'error');});});$('[data-take]',selEl).addEventListener('click',function(){var id=$('[data-give-badge]',selEl).value;if(!id)return;call('PUT','/users/'+u.id+'/badges/'+encodeURIComponent(id),{enabled:false}).then(function(){amsg('Badge hidden on their page (they still own it).','ok');}).catch(function(e){amsg(e.message,'error');});});var sb=$('[data-suspend]',selEl);if(sb)sb.addEventListener('click',function(){var reason=$('[data-susp-reason]',selEl).value.trim();if(!confirm('Suspend '+(u.username||u.id)+'?'))return;call('PATCH','/users/'+u.id+'/suspension',{suspended:true,reason:reason||null,until:null}).then(function(){amsg('Suspended.','ok');u.suspended_at=new Date().toISOString();u.suspension_reason=reason;select(u);loadUsers(false);}).catch(function(e){amsg(e.message,'error');});});var ub=$('[data-unsuspend]',selEl);if(ub)ub.addEventListener('click',function(){call('PATCH','/users/'+u.id+'/suspension',{suspended:false,reason:null,until:null}).then(function(){amsg('Suspension lifted.','ok');u.suspended_at=null;u.suspension_reason=null;u.suspended_until=null;select(u);loadUsers(false);}).catch(function(e){amsg(e.message,'error');});});$('[data-delete-user]',selEl).addEventListener('click',function(){var name=u.username||u.email||u.id;if(prompt('Delete '+name+' — their page, uploads and analytics, for good. Type the name to confirm:')!==name)return;call('DELETE','/users/'+u.id).then(function(){amsg('Deleted '+name+'.','ok');select(null);loadUsers(false);loadAudit();}).catch(function(e){amsg(e.message,'error');});});$('[data-close]',selEl).addEventListener('click',function(){var back=this.__opener||$('tr[data-uid="'+esc(u.id)+'"] [data-pick]',usersEl);select(null);if(back)back.focus();});selEl.scrollIntoView({block:'nearest'});}
var entEl=$('[data-entitlements]',adm);function loadEntitlements(){call('GET','/entitlements').then(function(d){entEl.innerHTML=table(['user','plan','active','expires','granted by','when'],(d.entitlements||[]).map(function(e){return'<tr><td>'+who(e.user_id)+'</td><td><b>'+esc(e.plan)+'</b></td><td class="'+(e.active?'ok':'bad')+'">'+(e.active?'yes':'no')+'</td><td>'+(e.expires_at?when(e.expires_at):'never')+'</td><td>'+who(e.granted_by)+'</td><td>'+when(e.created_at)+'</td></tr>';}),'no grants yet.');}).catch(fail(entEl));}
var bEl=$('[data-badges]',adm);function loadBadges(){call('GET','/badges').then(function(d){badgeList=d.badges||[];bEl.innerHTML=table(['badge','id','description','color'],badgeList.map(function(b){return'<tr><td><b>'+esc(b.name)+'</b></td><td><code>'+esc(b.id)+'</code></td><td>'+esc(b.description||'')+'</td><td><span class="badge-row__dot" style="--bc:'+esc(b.color||'#9b87f5')+';width:22px;height:22px;display:inline-grid"></span> <code>'+esc(b.color||'')+'</code></td></tr>';}),'no badges yet — create one above.');if(selected)select(selected);}).catch(fail(bEl));}
$('[data-badge-form]',adm).addEventListener('submit',function(e){e.preventDefault();var f=this;call('POST','/badges',{id:f.id.value.trim(),name:f.name.value.trim(),description:f.description.value.trim(),color:f.color.value}).then(function(){amsg('Badge created.','ok');f.reset();loadBadges();}).catch(function(e){amsg(e.message,'error');});});$$('[data-badge-form] [data-color-code]',adm).forEach(function(c){var inp=c.parentElement.querySelector('input');inp.addEventListener('input',function(){c.textContent=inp.value;});});var rEl=$('[data-reserved]',adm);function loadReserved(){call('GET','/reserved-usernames').then(function(d){var list=d.usernames||d.reserved||d.reserved_usernames||[];rEl.innerHTML=table(['name','reason','since',''],list.map(function(r){return'<tr><td><b>misa.lol/'+esc(r.username)+'</b></td><td>'+esc(r.reason||'')+'</td><td>'+when(r.created_at)+'</td><td><button class="btn btn--ghost btn--sm" type="button" data-unreserve="'+esc(r.username)+'">Release</button></td></tr>';}),'nothing reserved beyond the built-in list.');$$('[data-unreserve]',rEl).forEach(function(b){b.addEventListener('click',function(){call('DELETE','/reserved-usernames/'+encodeURIComponent(b.dataset.unreserve)).then(function(){amsg('Released '+b.dataset.unreserve+'.','ok');loadReserved();}).catch(function(e){amsg(e.message,'error');});});});}).catch(fail(rEl));}
$('[data-reserved-form]',adm).addEventListener('submit',function(e){e.preventDefault();var f=this;call('POST','/reserved-usernames',{username:f.username.value.trim().toLowerCase(),reason:f.reason.value.trim()||null}).then(function(){amsg('Reserved.','ok');f.reset();loadReserved();}).catch(function(e){amsg(e.message,'error');});});var repEl=$('[data-reports]',adm),STATUSES=['open','reviewed','resolved','dismissed'];function loadReports(){call('GET','/reports').then(function(d){repEl.innerHTML=table(['target','reason','details','status','when'],(d.reports||[]).map(function(r){return'<tr><td><b>'+esc(r.target_username||short(r.target_user_id))+'</b></td><td>'+esc(r.reason||'')+'</td><td title="'+esc(r.details||'')+'">'+esc((r.details||'').slice(0,60))+'</td><td><select class="field__input" data-report="'+esc(r.id)+'" aria-label="Status">'+STATUSES.map(function(s){return'<option'+(s===r.status?' selected':'')+'>'+s+'</option>';}).join('')+'</select></td><td>'+when(r.created_at)+'</td></tr>';}),'no reports. nice.');$$('[data-report]',repEl).forEach(function(sel){sel.addEventListener('change',function(){call('PATCH','/reports/'+sel.dataset.report,{status:sel.value}).then(function(){amsg('Report marked '+sel.value+'.','ok');}).catch(function(e){amsg(e.message,'error');});});});}).catch(fail(repEl));}
var flEl=$('[data-flags]',adm);function loadFlags(){call('GET','/feature-flags').then(function(d){flEl.innerHTML=table(['flag','description','on',''],(d.flags||[]).map(function(f){return'<tr><td><code>'+esc(f.key)+'</code></td><td>'+esc(f.description||'')+'</td><td class="'+(f.enabled?'ok':'bad')+'">'+(f.enabled?'on':'off')+'</td><td><button class="btn btn--ghost btn--sm" type="button" data-flag="'+esc(f.key)+'" data-on="'+(f.enabled?'1':'0')+'" data-desc="'+esc(f.description||'')+'">'+(f.enabled?'Turn off':'Turn on')+'</button></td></tr>';}),'no flags defined.');$$('[data-flag]',flEl).forEach(function(b){b.addEventListener('click',function(){call('PUT','/feature-flags/'+encodeURIComponent(b.dataset.flag),{enabled:b.dataset.on!=='1',description:b.dataset.desc}).then(function(){loadFlags();}).catch(function(e){amsg(e.message,'error');});});});}).catch(fail(flEl));}
var auEl=$('[data-audit]',adm);function loadAudit(){call('GET','/audit-logs').then(function(d){auEl.innerHTML=table(['when','actor','action','target','details'],(d.logs||[]).slice(0,100).map(function(l){var meta=l.metadata;if(meta&&typeof meta==='object')meta=JSON.stringify(meta);return'<tr><td>'+when(l.created_at)+'</td><td>'+who(l.actor_user_id)+'</td><td><b>'+esc(l.action)+'</b></td><td>'+esc((l.target_type||'')+' '+short(l.target_id))+'</td><td title="'+esc(String(meta||''))+'">'+esc(String(meta||'').slice(0,60))+'</td></tr>';}),'nothing logged yet.');}).catch(fail(auEl));}
function loadAll(){call('GET','/users?search=&limit=50&offset=0').then(function(d){lastUsers=d.users||[];userOffset=50;renderUsers(lastUsers,false);}).catch(fail(usersEl)).then(function(){loadEntitlements();loadBadges();loadReserved();loadReports();loadFlags();loadAudit();});}
var rf=$('[data-adm-refresh]');if(rf)rf.addEventListener('click',loadAll);(ADEMO?Promise.resolve({is_admin:true,username:'valer',display_name:'valer'}):fetch('/api/v1/me',{credentials:'include',headers:{Accept:'application/json'}}).then(function(r){return r.ok?r.json():null;})).then(function(u){if(!u||!u.is_admin){location.href='/dashboard';return;}var nn=$('#nav-name');if(nn&&ADEMO)nn.textContent=u.display_name;loadAll();}).catch(function(){amsg('Could not load.','error');});var anav=$$('.dash__nav a',adm),asecs=$$('.dash__sec',adm);if('IntersectionObserver'in window){var aspy=new IntersectionObserver(function(en){en.forEach(function(x){if(x.isIntersecting)anav.forEach(function(a){a.classList.toggle('is-active',a.getAttribute('href')==='#'+x.target.id);});});},{rootMargin:'-15% 0px -70% 0px'});asecs.forEach(function(s){aspy.observe(s);});}}
var claimIn=$('#claim-name');if(claimIn&&$('.hero3')&&!reduce){var names=['yourname','ren','peach','void','n0t_found','lune','kuro'],ni=0,pTimer=null,pAlive=true;function stopPlaceholder(){pAlive=false;clearTimeout(pTimer);claimIn.placeholder='yourname';}
(function cycle(){if(!pAlive)return;if(document.activeElement===claimIn||claimIn.value){stopPlaceholder();return;}
var w=names[ni%names.length],k=0;ni++;(function type(){if(!pAlive)return;claimIn.placeholder=w.slice(0,++k);if(k<w.length)pTimer=setTimeout(type,90);else pTimer=setTimeout(function(){var d=w.length;(function del(){if(!pAlive)return;claimIn.placeholder=w.slice(0,--d);if(d>0)pTimer=setTimeout(del,40);else pTimer=setTimeout(cycle,400);})();},1800);})();})();claimIn.addEventListener('focus',stopPlaceholder);claimIn.addEventListener('input',stopPlaceholder);addEventListener('visibilitychange',function(){if(document.hidden)stopPlaceholder();});}
var stage=$('.hero3__stage'),heroPhone=stage&&$('.phone',stage);if(stage&&heroPhone&&!reduce&&matchMedia('(hover:hover) and (pointer:fine) and (min-width:941px)').matches){var tiltRaf=null,tiltX=0,tiltY=0;stage.addEventListener('pointermove',function(e){var r=heroPhone.getBoundingClientRect();if(r.width<10||r.height<10)return;tiltX=Math.max(-1,Math.min(1,((e.clientX-r.left)/r.width-.5)*2)); tiltY=Math.max(-1,Math.min(1,((e.clientY-r.top)/r.height-.5)*2));if(tiltRaf)return;tiltRaf=requestAnimationFrame(function(){tiltRaf=null;heroPhone.classList.add('is-tilting');heroPhone.style.transform='rotateY('+(-16+tiltX*10).toFixed(1)+'deg) rotateX('+(4-tiltY*8).toFixed(1)+'deg) rotate(3deg)';});});stage.addEventListener('pointerleave',function(){heroPhone.classList.remove('is-tilting');heroPhone.style.transform='';});}
var realSec=$('[data-real-pages]'),realFilm=$('#real-film');if(realSec&&realFilm&&ON_SITE&&window.fetch){fetch('/api/v1/explore',{headers:{Accept:'application/json'}}).then(function(r){return r.ok?r.json():null;}).then(function(d){var pages=(d&&d.pages)||[];if(!pages.length)return;realFilm.innerHTML=pages.map(function(pg){var links=(pg.links||[]).map(function(l){return'<span class="pf__link">'+ic('link')+'<span class="pf__label">'+esc(l)+'</span>'+ic('arrow')+'</span>';}).join('');var vars='--pf-bg:'+esc(pg.bg)+';--pf-fg:'+esc(pg.text)+';--pf-mute:color-mix(in srgb,'+esc(pg.text)+' 62%,transparent);--pf-acc:'+esc(pg.accent)+';--pf-badge:'+esc(pg.accent)+';--pf-av:linear-gradient(135deg,'+esc(pg.accent)+',rgba(0,0,0,.55))';return'<a class="film__item real-page" href="/'+esc(pg.username)+'" aria-label="Open '+esc(pg.name||pg.username)+'’s page — misa.lol/'+esc(pg.username)+'">'+'<div class="phone phone--sm" aria-hidden="true" style="'+vars+'"><div class="phone__notch"></div><div class="phone__bg"></div><div class="phone__glare"></div><div class="pf"><div class="pf__avatar">'+esc((pg.name||pg.username).charAt(0).toLowerCase())+'</div>'+'<div class="pf__name"><span>'+esc(pg.name||pg.username)+'</span>'+(pg.badge?'<span class="pf__badge">'+ic('check')+'</span>':'')+'</div><div class="pf__handle">misa.lol/'+esc(pg.username)+'</div><p class="pf__bio">'+esc(pg.bio||'')+'</p>'+'<div class="pf__links">'+links+'</div><div class="pf__foot"><span class="pf__views">'+ic('eye')+esc(String(pg.views||0))+' views</span></div></div></div>'+'<span class="film__cap hand">'+esc(pg.name||pg.username)+'’s page <small>real</small></span></a>';}).join('');realSec.hidden=false;}).catch(function(){});}
var kitPhones=$('[data-kit-phones]');if(kitPhones){var seen={};kitPhones.innerHTML=PAGES.filter(function(p){if(seen[p.theme])return false;seen[p.theme]=true;return true;}).map(function(p){return'<div>'+phoneHTML(p)+capHTML(p)+'</div>';}).join('');}
var kitIcons=$('[data-kit-icons]');if(kitIcons){kitIcons.innerHTML=$$('symbol[id^="i-"]').map(function(s){var n=s.id.slice(2);return'<div class="kit__icon">'+ic(n)+'<span>'+n+'</span></div>';}).join('');}
var logEl=$('#log-list'),U=window.MISA_UPDATES;if(logEl&&U&&!logEl.children.length){logEl.innerHTML=U.updates.map(function(u){var links=(u.links||[]).map(function(l){return'<a class="chip" href="'+esc(l[1])+'">'+ic('arrow')+esc(l[0])+'</a>';}).join('');var files=(u.files||[]).map(function(f){return'<code>'+esc(f)+'</code>';}).join(' ');return'<article class="log__item reveal"><div class="log__ver">'+esc(u.ver)+'<small>'+esc(u.date)+(u.time?' · '+esc(u.time):'')+'</small></div>'+'<div class="log__body"><h2 class="log__title">'+esc(u.title)+'</h2>'+u.lines.map(function(l){return'<p>'+l+'</p>';}).join('')+
(links?'<div class="log__links">'+links+'</div>':'')+(files?'<p class="log__files">drop-in: '+files+'</p>':'')+'</div></article>';}).join('');var nx=$('#next-list');if(nx)nx.innerHTML=U.roadmap.map(function(r){return'<li>'+esc(r)+'</li>';}).join('');var td=$('#todo-list');if(td)td.innerHTML=U.todos.map(function(t){return'<li>'+esc(t)+'</li>';}).join('');}
var legalNav=$('.legal__nav');if(legalNav&&'IntersectionObserver'in window){var navLinks=$$('a[href^="#"]',legalNav),secs=$$('.legal__sec');function markActive(id){navLinks.forEach(function(a){a.classList.toggle('is-active',a.getAttribute('href')==='#'+id);});}
var spy=new IntersectionObserver(function(entries){entries.forEach(function(en){if(en.isIntersecting)markActive(en.target.id);});},{rootMargin:'-20% 0px -65% 0px',threshold:0});secs.forEach(function(s){spy.observe(s);});if(secs[0]){markActive(secs[0].id);addEventListener('scroll',function(){if(scrollY<secs[0].offsetTop-innerHeight*.35)markActive(secs[0].id);},{passive:true});}}
var tw=$('[data-tw]');if(tw){if(reduce)tw.textContent='music';else makeTyper(tw,['music','zines','noise','mistakes','pages']);}
var els=$$('.reveal');els.forEach(function(el){if(el.getBoundingClientRect().top<innerHeight+40)el.classList.add('in');});html.classList.add('js','js-snap');requestAnimationFrame(function(){requestAnimationFrame(function(){html.classList.remove('js-snap');});});if('IntersectionObserver'in window&&!reduce){var io=new IntersectionObserver(function(entries){entries.forEach(function(en){if(en.isIntersecting){en.target.classList.add('in');io.unobserve(en.target);}});},{rootMargin:'0px 0px -6% 0px',threshold:.04});els.forEach(function(el){io.observe(el);});setTimeout(function(){els.forEach(function(el){if(!el.classList.contains('in')&&el.getBoundingClientRect().top<innerHeight)el.classList.add('in');});},1200);}else{els.forEach(function(el){el.classList.add('in');});}
if(reduce){$$('video[autoplay]').forEach(function(v){v.removeAttribute('autoplay');v.pause();});}})();