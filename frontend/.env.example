# Server-only URL for the FastAPI service.
# In the combined Docker deployment, app01 is the internal FastAPI service name.
MISA_BACKEND_URL=http://app01:8000

# Optional; used by the original HTML auth pages through /js/config.js.
MISA_TURNSTILE_SITE_KEY=
